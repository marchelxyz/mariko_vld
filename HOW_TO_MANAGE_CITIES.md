# 🎯 Как управлять городами - ДЛЯ ВАС

## ✅ Готово! Система работает

Теперь когда вы **деактивируете город в админ-панели** и обновите конфигурацию - он исчезнет для **ВСЕХ пользователей** приложения!

---

## 🚀 Как это работает сейчас

### Шаг 1: Откройте админ-панель
1. Нажмите на 🛡️ **Админ-панель** в нижней навигации
2. Выберите **Управление ресторанами**

### Шаг 2: Измените статус города
1. Найдите нужный город (например, Жуковский)
2. Нажмите кнопку **Активировать/Деактивировать**
3. Вы увидите всплывающее окно с инструкцией

### Шаг 3: Обновите конфигурацию
1. Откройте файл: **`src/shared/config/activeCities.ts`**
2. Найдите массив `ACTIVE_CITY_IDS`
3. **Чтобы скрыть город** - удалите его ID из массива
4. **Чтобы показать город** - добавьте его ID в массив

Пример:
```typescript
// БЫЛО (3 города активны):
export const ACTIVE_CITY_IDS: string[] = [
  "zhukovsky",
  "kaluga",
  "penza",
];

// СТАЛО (только 2 города активны, Жуковский скрыт):
export const ACTIVE_CITY_IDS: string[] = [
  "kaluga",
  "penza",
];
```

### Шаг 4: Соберите и задеплойте
```bash
# В терминале
npm run build

# Загрузите папку dist/ на сервер TimeWeb
```

---

## 📱 Что увидят пользователи

### ✅ До деплоя (у вас локально)
- Вы сразу увидите изменения после сохранения файла
- В режиме `npm run dev` изменения применяются мгновенно

### 🌍 После деплоя (все пользователи)
- Пользователи увидят только активные города
- Скрытые города исчезнут из:
  - Выбора города на главной
  - Списка ресторанов
  - Навигации по доставке

---

## 🎯 Текущая конфигурация

**Файл**: `src/shared/config/activeCities.ts`

**Активные города сейчас**:
- ✅ Жуковский (`zhukovsky`)
- ✅ Калуга (`kaluga`)
- ✅ Пенза (`penza`)

**Все остальные 21 город** скрыты от пользователей.

---

## 💡 Частые задачи

### Активировать Нижний Новгород

```typescript
export const ACTIVE_CITY_IDS: string[] = [
  "zhukovsky",
  "kaluga",
  "penza",
  "nizhny-novgorod",  // ← Добавьте эту строку
];
```

### Активировать Санкт-Петербург

```typescript
export const ACTIVE_CITY_IDS: string[] = [
  "zhukovsky",
  "kaluga",
  "penza",
  "saint-petersburg",  // ← Добавьте эту строку
];
```

### Деактивировать Пензу

```typescript
export const ACTIVE_CITY_IDS: string[] = [
  "zhukovsky",
  "kaluga",
  // "penza",  // ← Удалите или закомментируйте
];
```

### Активировать ВСЕ города

```typescript
// В начале файла замените:
export const USE_ACTIVE_CITIES_FILTER = false;  // ← Поставьте false
```

---

## 📋 Список всех городов с ID

**Используйте эти ID** в массиве `ACTIVE_CITY_IDS`:

```
nizhny-novgorod      - Нижний Новгород
saint-petersburg     - Санкт-Петербург
kazan               - Казань
kemerovo            - Кемерово
tomsk               - Томск
smolensk            - Смоленск
kaluga              - Калуга ✅
samara              - Самара
novosibirsk         - Новосибирск
magnitogorsk        - Магнитогорск
balakhna            - Балахна
kstovo              - Кстово
lesnoy-gorodok      - Лесной Городок
novorossiysk        - Новороссийск
zhukovsky           - Жуковский ✅
odintsovo           - Одинцово
neftekamsk          - Нефтекамск
penza               - Пенза ✅
astana              - Астана
atyrau              - Атырау
volgograd           - Волгоград
bugulma             - Бугульма
ufa                 - Уфа
saransk             - Саранск
```

---

## 🔍 Проверка

### Как проверить что работает?

1. **Локально** (`npm run dev`):
   - Откройте главную страницу
   - Нажмите на выбор города
   - Вы должны видеть только активные города

2. **В админ-панели**:
   - Зайдите в Управление ресторанами
   - Вы видите ВСЕ города (включая неактивные)
   - Неактивные помечены бейджем "Деактивирован"

3. **В консоли** (F12):
```javascript
// Скопируйте и вставьте в консоль браузера
import { getAvailableCities } from './src/shared/data/cities';
console.log('Активные города:', getAvailableCities());
```

---

## ⚠️ Важно помнить

### ❌ Изменения НЕ применяются автоматически для пользователей
После редактирования `activeCities.ts` **обязательно**:
1. `npm run build` - собрать проект
2. Загрузить `dist/` на TimeWeb сервер
3. Пользователи увидят изменения после обновления страницы

### ✅ Изменения применяются мгновенно локально
В режиме разработки (`npm run dev`) изменения видны сразу

### 🔄 Кэш браузера
Если пользователи не видят изменения:
- Попросите обновить страницу (Ctrl+F5 или Cmd+R)
- Или очистить кэш Telegram

---

## 🚀 Деплой на TimeWeb

### Быстрый способ (через панель):
1. Войдите в панель TimeWeb
2. Откройте **Файловый менеджер**
3. Перейдите в папку сайта
4. Загрузите файлы из `dist/`

### Через FTP:
1. Подключитесь через FileZilla
2. Перейдите в `/public_html` (или `/domains/ваш_домен`)
3. Загрузите содержимое `dist/`

### Через Git (рекомендуется):
```bash
# На компьютере
git add src/shared/config/activeCities.ts
git commit -m "Обновлены активные города"
git push origin main

# На сервере (SSH)
cd /path/to/project
git pull
npm run build
# Скопируйте dist/ в папку сайта
```

---

## 🔮 Будущее

В будущем планируется **автоматическая система** с backend:
- ✅ Нажали кнопку в админке → изменения применились для всех
- ✅ Без сборки и деплоя
- ✅ История изменений
- ✅ Откат изменений одной кнопкой

Но сейчас работает текущая система через конфигурационный файл.

---

## 📞 Нужна помощь?

1. **Полная документация**: `docs/DEPLOY_CITY_CHANGES.md`
2. **Краткая инструкция**: `ADMIN_CITIES_GUIDE.md`
3. **Админ-панель**: `docs/ADMIN_PANEL.md`

---

**Всё готово! Можете управлять городами! 🎉**

