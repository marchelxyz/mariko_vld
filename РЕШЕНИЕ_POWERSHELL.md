# 🚀 Быстрое решение проблемы с PowerShell

## Проблема
PowerShell не может правильно обработать команды git из-за кириллических символов в пути:
```
c:\Users\Роман\OneDrive\Desktop\Договора НПД Мазов\...
```

## ✅ РЕШЕНИЕ 1: Использовать Git Bash (САМОЕ ПРОСТОЕ)

**Это самое надежное решение!**

1. Откройте **Git Bash** (не PowerShell)
2. Перейдите в директорию проекта:
   ```bash
   cd "/c/Users/Роман/OneDrive/Desktop/Договора НПД Мазов/03/Проект Марико апп/Код для курсора/mariko_vld"
   ```
3. Выполните команды git - они будут работать без проблем!

**Как открыть Git Bash:**
- Нажмите `Win + R`
- Введите `git-bash` и нажмите Enter
- Или найдите "Git Bash" в меню Пуск

## ✅ РЕШЕНИЕ 2: Настроить PowerShell один раз (навсегда)

### Шаг 1: Откройте профиль PowerShell

В PowerShell выполните:
```powershell
notepad $PROFILE
```

Если файл не существует, PowerShell предложит создать его - согласитесь.

### Шаг 2: Добавьте эти строки в файл

```powershell
# Устанавливаем кодировку UTF-8 при запуске PowerShell
[Console]::OutputEncoding = [System.Text.Encoding]::UTF8
[Console]::InputEncoding = [System.Text.Encoding]::UTF8
$OutputEncoding = [System.Text.Encoding]::UTF8
chcp 65001 | Out-Null
```

### Шаг 3: Сохраните и перезапустите PowerShell

- Нажмите `Ctrl+S` для сохранения
- Закройте и откройте PowerShell заново

**Готово!** Теперь PowerShell будет работать с кириллицей.

## ✅ РЕШЕНИЕ 3: Использовать исправленный скрипт

Если не хотите настраивать профиль, используйте готовый скрипт:

```powershell
.\create_pr_fixed.ps1
```

Этот скрипт автоматически устанавливает правильную кодировку перед выполнением команд.

## ✅ РЕШЕНИЕ 4: Использовать универсальный скрипт в Git Bash

В Git Bash выполните:

```bash
./create_pr_universal.sh feat/booking-cart-notification "feat: добавлено уведомление о передаче собранного меню в ресторан при бронировании"
```

## 📋 Быстрая проверка

После настройки проверьте кодировку:

```powershell
[Console]::OutputEncoding
chcp
```

Должно быть:
- `System.Text.UTF8Encoding`
- `65001`

## 🎯 Рекомендация

**Используйте Git Bash** - это самое простое и надежное решение. В Git Bash все команды git работают без проблем с кириллицей.

## 📝 Пример использования Git Bash

```bash
# Перейти в проект
cd "/c/Users/Роман/OneDrive/Desktop/Договора НПД Мазов/03/Проект Марико апп/Код для курсора/mariko_vld"

# Проверить статус
git status

# Добавить изменения
git add .

# Создать коммит
git commit -m "feat: добавлено уведомление о передаче меню"

# Создать ветку и отправить
git checkout -b feat/booking-cart-notification
git push -u origin feat/booking-cart-notification

# Создать PR
gh pr create --base main --head feat/booking-cart-notification --title "feat: добавлено уведомление о передаче собранного меню в ресторан при бронировании" --body-file PR_DESCRIPTION.md --web
```

Или просто используйте скрипт:
```bash
./create_pr_universal.sh feat/booking-cart-notification "feat: добавлено уведомление о передаче собранного меню в ресторан при бронировании"
```
