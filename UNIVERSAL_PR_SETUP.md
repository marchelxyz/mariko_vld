# Универсальная система создания PR для всех репозиториев

## Текущая ситуация

Скрипты `auto_create_pr.sh` и `auto_create_pr.ps1` работают **только в текущем репозитории**, где они находятся.

## Решение: Универсальный скрипт

Создан универсальный скрипт `create_pr_universal.sh`, который можно использовать в **любом git репозитории**.

## Установка глобального скрипта

### Шаг 1: Установить скрипт глобально

Выполните в Git Bash:

```bash
./setup_global_pr.sh
```

Этот скрипт:
- Скопирует `create_pr_universal.sh` в `~/.local/bin/pr`
- Добавит директорию в PATH
- Сделает команду `pr` доступной везде

### Шаг 2: Перезагрузить профиль

```bash
source ~/.bashrc
```

Или просто закройте и откройте Git Bash заново.

## Использование в любом репозитории

После установки вы можете использовать команду `pr` в **любом git репозитории**:

### Базовое использование

```bash
# В любом git репозитории
cd /path/to/any/repo
pr
```

Скрипт автоматически:
- Определит текущую ветку
- Создаст название PR из названия ветки
- Создаст и откроет PR

### С указанием ветки

```bash
pr feat/my-feature
```

### С указанием ветки и названия

```bash
pr feat/my-feature "feat: моя новая функция"
```

### С указанием базовой ветки

```bash
pr feat/my-feature "feat: описание" develop
```

## Как это работает

1. **Автоматическое определение репозитория:**
   - Скрипт проверяет, что вы в git репозитории
   - Использует текущую директорию

2. **Автоматическое определение ветки:**
   - Если ветка не указана, использует текущую ветку
   - Если ветки нет, создает новую

3. **Автоматическое название PR:**
   - Если название не указано, генерирует из названия ветки
   - `feat/my-feature` → `feat: my feature`
   - `fix/bug-123` → `fix: bug 123`

## Примеры использования в разных репозиториях

### Репозиторий 1: mariko_vld

```bash
cd ~/projects/mariko_vld
pr feat/new-feature "feat: новая функция"
```

### Репозиторий 2: другой проект

```bash
cd ~/projects/other-project
pr fix/bug "fix: исправление бага"
```

### Репозиторий 3: еще один проект

```bash
cd ~/projects/third-project
pr  # Использует текущую ветку автоматически
```

## Преимущества универсального скрипта

✅ **Работает везде** - в любом git репозитории  
✅ **Автоматическое определение** - ветка и название определяются автоматически  
✅ **Одна команда** - просто `pr`  
✅ **Гибкость** - можно указать параметры при необходимости  

## Ручная установка (если скрипт установки не работает)

1. **Скопируйте скрипт:**
```bash
mkdir -p ~/.local/bin
cp create_pr_universal.sh ~/.local/bin/pr
chmod +x ~/.local/bin/pr
```

2. **Добавьте в PATH:**
Добавьте в `~/.bashrc`:
```bash
export PATH="$PATH:$HOME/.local/bin"
```

3. **Перезагрузите:**
```bash
source ~/.bashrc
```

## Проверка установки

```bash
# Проверить, что команда доступна
which pr

# Проверить версию (должен показать путь к скрипту)
pr --help 2>&1 || echo "Скрипт установлен"
```

## Резюме

**До установки:** Скрипты работают только в текущем репозитории  
**После установки:** Команда `pr` работает в **любом git репозитории**

Установите один раз - используйте везде! 🚀
