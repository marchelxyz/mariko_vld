#!/usr/bin/env bash
# Конфиг деплоя. Скопируй в .env.deploy и поправь значения.

# SSH хост (user@host)
export SERVER_HOST="root@85.198.83.72"

# Куда складывать собранный фронт (nginx root)
export WEB_ROOT="/var/www/html"

# Пути проекта на сервере
export REMOTE_PROJECT_ROOT="/root/HM-projecttt"
export REMOTE_FRONTEND_DIR="$REMOTE_PROJECT_ROOT/frontend"
export REMOTE_BOT_DIR="$REMOTE_PROJECT_ROOT/backend/bot"
export REMOTE_SERVER_DIR="$REMOTE_PROJECT_ROOT/backend/server"

# PM2 процессы
export BOT_NAME="hachapuri-bot"
export CART_SERVER_NAME="cart-server"

# Опции SSH/rsync (по желанию)
export SSH_OPTS="-o StrictHostKeyChecking=no"
export RSYNC_OPTS="-avz"
