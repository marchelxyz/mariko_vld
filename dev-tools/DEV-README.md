# 🛠️ Инструкция для разработчиков

## Хачапури Марико - Telegram Mini App

### 🚀 Быстрый старт разработки

#### 1. Запуск локального сервера для тестирования:
```bash
./dev-tools/start-dev.sh
```

**Что делает этот скрипт:**
- Проверяет наличие зависимостей и устанавливает их при необходимости
- Проверяет, что проект компилируется без ошибок
- Запускает dev-сервер на свободном порту (обычно 8081)
- Показывает список страниц для тестирования

#### 2. Полная проверка перед деплоем:
```bash
./dev-tools/check-before-deploy.sh
```

**Что делает этот скрипт:**
- Проверяет TypeScript ошибки
- Запускает линтер
- Проверяет сборку проекта
- Анализирует размер бандла
- Проверяет наличие важных файлов
- Показывает следующие шаги для деплоя

### 📁 Структура папки dev-tools

```
dev-tools/
├── start-dev.sh           # Запуск dev-сервера
├── check-before-deploy.sh # Проверка перед деплоем
└── DEV-README.md         # Эта документация
```

### 📋 Чек-лист для тестирования изменений

**Обязательные страницы для проверки:**

✅ **Главная страница** (`/`)
- Город отображается справа сверху (без адреса)
- Адреса всех ресторанов показаны внизу страницы
- Все кнопки работают

✅ **Страница профиля** (`/profile`)
- Только логотип сверху (без города/адреса)
- Профиль загружается и редактируется

✅ **Редактирование профиля** (`/edit-profile`)
- Только логотип сверху
- Все поля можно редактировать и сохранять

✅ **Страница акций** (`/promotions`)
- Только логотип сверху
- Акции отображаются корректно

✅ **Страница доставки** (`/delivery`)
- Только логотип сверху
- Все варианты доставки работают

✅ **Страница отзывов** (`/review`)
- Форма отзывов работает
- Валидация полей
- Разное поведение для положительных/отрицательных отзывов

✅ **Бронирование** (`/booking`)
- Форма бронирования работает
- Все поля валидируются

### 🔄 Рабочий процесс

1. **Разработка:**
   ```bash
   ./dev-tools/start-dev.sh
   ```

2. **Тестирование:**
   - Проверить все страницы из чек-листа
   - Протестировать на мобильном размере экрана
   - Проверить работу форм

3. **Перед деплоем:**
   ```bash
   ./dev-tools/check-before-deploy.sh
   ```

4. **Деплой:**
   ```bash
   git add .
   git commit -m "описание изменений"
   git push origin main
   ```

5. **Проверка продакшена:**
   - Открыть https://hachapurimariko.netlify.app
   - Проверить ключевые функции

### 🎯 Полезные команды

```bash
# Запуск dev-сервера из корня проекта
./dev-tools/start-dev.sh

# Проверка перед деплоем из корня проекта
./dev-tools/check-before-deploy.sh

# Или стандартные команды:
npm run dev          # Только запуск dev-сервера
npm run build        # Только сборка проекта
npx tsc --noEmit     # Проверка TypeScript
npm install          # Установка зависимостей
```

### 📱 Тестирование в Telegram

1. Откройте бота в Telegram
2. Нажмите кнопку "Открыть приложение"
3. Или используйте команду `/start`

### 🐛 Отладка

Если что-то не работает:

1. Проверьте консоль браузера (F12)
2. Убедитесь, что dev-сервер запущен
3. Проверьте, что нет TypeScript ошибок
4. Перезапустите сервер

### 📞 Поддержка

При возникновении проблем:
- Проверьте логи в консоли
- Убедитесь, что все зависимости установлены
- Попробуйте перезапустить сервер

### 🔧 Быстрые команды

```bash
# Из корня проекта:
./dev-tools/start-dev.sh        # Запуск разработки
./dev-tools/check-before-deploy.sh  # Проверка перед деплоем
``` 