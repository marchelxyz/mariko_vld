# Подробная инструкция по созданию инфраструктуры Yandex Cloud Storage

## 📋 Что мы будем создавать

1. **Облако (Cloud)** - контейнер для всех ресурсов
2. **Бакет (Bucket)** - хранилище для файлов
3. **Сервисный аккаунт** - для безопасного доступа к хранилищу
4. **Статические ключи доступа** - для авторизации в API

---

## Шаг 1: Регистрация и создание облака

### 1.1. Регистрация в Yandex Cloud

1. Перейдите на [https://console.cloud.yandex.ru/](https://console.cloud.yandex.ru/)
2. Войдите через Яндекс ID или создайте новый аккаунт
3. При первом входе вам предложат создать платежный аккаунт

### 1.2. Создание облака (Cloud)

1. В консоли нажмите на выпадающий список вверху (где написано "Облако" или "Cloud")
2. Нажмите **"Создать облако"** или **"Create Cloud"**
3. Заполните форму:
   - **Название**: `mariko-cloud` (или любое другое)
   - **Организация**: выберите существующую или создайте новую
4. Нажмите **"Создать"**

**Важно**: Запишите ID облака (он понадобится для некоторых операций)

---

## Шаг 2: Создание бакета (Bucket)

### 2.1. Открытие Object Storage

1. В левом меню найдите раздел **"Object Storage"** или **"Объектное хранилище"**
2. Если это первый раз, нажмите **"Подключить"** или **"Enable"**
3. Примите условия использования

### 2.2. Создание бакета

1. Нажмите кнопку **"Создать бакет"** или **"Create Bucket"**

2. **Заполните параметры:**

   **Имя бакета:**
   - Введите уникальное имя: `mariko-storage`
   - ⚠️ Имя должно быть уникальным во всем Yandex Cloud
   - Может содержать только строчные латинские буквы, цифры и дефисы
   - Длина: 3-63 символа

   **Регион:**
   - Выберите: `ru-central1` (Москва) - самый популярный
   - Или `ru-central1-a`, `ru-central1-b`, `ru-central1-c` для конкретной зоны доступности

   **Тип доступа:**
   - Выберите **"Приватный"** (Private)
   - Публичный доступ можно включить позже в настройках

   **Класс хранилища:**
   - Выберите **"Standard"** (для частого доступа)
   - Coldline и Ice - для архивов (дешевле, но медленнее)

   **Версионирование:**
   - Оставьте выключенным (можно включить позже)

   **Шифрование:**
   - По умолчанию включено (рекомендуется)

3. Нажмите **"Создать бакет"**

### 2.3. Настройка публичного доступа (опционально)

Если вы хотите, чтобы изображения были доступны напрямую по URL:

1. Откройте созданный бакет `mariko-storage`
2. Перейдите во вкладку **"Настройки"** или **"Settings"**
3. Найдите раздел **"Доступ"** или **"Access"**
4. Включите переключатель **"Публичный доступ на чтение"**
5. Нажмите **"Сохранить"**

**Важно**: 
- При публичном доступе все файлы будут доступны по прямому URL
- Убедитесь, что это соответствует вашим требованиям безопасности
- Для приватных файлов используйте подписанные URL (уже реализовано в коде)

---

## Шаг 3: Создание сервисного аккаунта

### 3.1. Открытие раздела сервисных аккаунтов

1. В левом меню найдите раздел **"Сервисные аккаунты"** или **"Service Accounts"**
2. Если его нет в меню, найдите через поиск вверху

### 3.2. Создание сервисного аккаунта

1. Нажмите **"Создать сервисный аккаунт"** или **"Create Service Account"**

2. Заполните форму:
   - **Имя**: `mariko-storage-service`
   - **Описание** (опционально): `Сервисный аккаунт для работы с Object Storage`
   - **Роль**: пока не выбирайте (назначим позже)

3. Нажмите **"Создать"**

### 3.3. Назначение роли сервисному аккаунту

1. Откройте созданный сервисный аккаунт `mariko-storage-service`
2. Перейдите на вкладку **"Роли"** или **"Roles"**
3. Нажмите **"Назначить роли"** или **"Assign Roles"**

4. В открывшемся окне:
   - Выберите **"Роль"** → найдите и выберите `storage.editor`
   - Эта роль дает полный доступ к Object Storage (чтение, запись, удаление)
   - Для более строгой безопасности можно использовать `storage.uploader` (только загрузка) + `storage.viewer` (только чтение)

5. Нажмите **"Сохранить"** или **"Assign"**

**Альтернативные роли:**
- `storage.editor` - полный доступ (рекомендуется для начала)
- `storage.uploader` - только загрузка файлов
- `storage.viewer` - только чтение файлов
- `storage.admin` - полный доступ + управление бакетами

---

## Шаг 4: Создание статического ключа доступа

### 4.1. Открытие раздела ключей

1. В сервисном аккаунте `mariko-storage-service` перейдите на вкладку **"Ключи"** или **"Keys"**

### 4.2. Создание статического ключа

1. Нажмите **"Создать новый ключ"** или **"Create New Key"**
2. Выберите **"Создать статический ключ доступа"** или **"Create Access Key"**
3. В открывшемся окне нажмите **"Создать"**

### 4.3. Сохранение ключей

⚠️ **КРИТИЧЕСКИ ВАЖНО**: 

После создания ключа вы увидите два значения:

1. **Access Key ID** (например: `YCAJxxxxxxxxxxxxxxxxxxxx`)
   - Это публичный идентификатор
   - Его можно увидеть позже в списке ключей

2. **Secret Access Key** (например: `YCMxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx`)
   - Это секретный ключ
   - ⚠️ **Показывается только один раз!**
   - **Обязательно скопируйте и сохраните в безопасном месте**

**Рекомендации по сохранению:**
- Сохраните в менеджер паролей (1Password, LastPass, Bitwarden)
- Или в зашифрованный файл
- НЕ коммитьте в Git
- НЕ отправляйте по email или мессенджерам

---

## Шаг 5: Настройка переменных окружения

### 5.1. Локальная настройка (для разработки)

Создайте или отредактируйте файл `backend/server/.env.local`:

```bash
# Yandex Object Storage
YANDEX_STORAGE_ACCESS_KEY_ID=YCAJxxxxxxxxxxxxxxxxxxxx
YANDEX_STORAGE_SECRET_ACCESS_KEY=YCMxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
YANDEX_STORAGE_BUCKET_NAME=mariko-storage
YANDEX_STORAGE_REGION=ru-central1
YANDEX_STORAGE_ENDPOINT=https://storage.yandexcloud.net
YANDEX_STORAGE_PUBLIC_URL=https://storage.yandexcloud.net/mariko-storage
```

**Где взять значения:**
- `YANDEX_STORAGE_ACCESS_KEY_ID` - из шага 4.3 (Access Key ID)
- `YANDEX_STORAGE_SECRET_ACCESS_KEY` - из шага 4.3 (Secret Access Key)
- `YANDEX_STORAGE_BUCKET_NAME` - имя бакета из шага 2.2 (`mariko-storage`)
- `YANDEX_STORAGE_REGION` - регион из шага 2.2 (`ru-central1`)
- `YANDEX_STORAGE_ENDPOINT` - всегда `https://storage.yandexcloud.net`
- `YANDEX_STORAGE_PUBLIC_URL` - только если включен публичный доступ (шаг 2.3)

### 5.2. Настройка на Railway (для production)

#### Вариант 1: Через скрипт (рекомендуется)

1. Убедитесь, что у вас настроен локальный `.env` файл (шаг 5.1)
2. Запустите скрипт:
   ```bash
   bash scripts/setup-railway-env.sh
   ```
3. Скрипт автоматически прочитает значения из `.env` и установит их на Railway

#### Вариант 2: Через Railway CLI

```bash
# Установите Railway CLI (если еще не установлен)
npm i -g @railway/cli

# Войдите в Railway
railway login

# Свяжите проект
railway link

# Установите переменные для backend сервиса
railway variables set YANDEX_STORAGE_ACCESS_KEY_ID=YCAJ... --service backend
railway variables set YANDEX_STORAGE_SECRET_ACCESS_KEY=YCM... --service backend
railway variables set YANDEX_STORAGE_BUCKET_NAME=mariko-storage --service backend
railway variables set YANDEX_STORAGE_REGION=ru-central1 --service backend
railway variables set YANDEX_STORAGE_ENDPOINT=https://storage.yandexcloud.net --service backend
railway variables set YANDEX_STORAGE_PUBLIC_URL=https://storage.yandexcloud.net/mariko-storage --service backend
```

#### Вариант 3: Через веб-интерфейс Railway

1. Откройте [Railway Dashboard](https://railway.app)
2. Выберите проект → сервис `backend`
3. Перейдите в **Variables**
4. Нажмите **New Variable** для каждой переменной
5. Введите имя и значение

---

## Шаг 6: Проверка подключения

### 6.1. Локальная проверка

1. Запустите backend сервер:
   ```bash
   cd backend
   npm run dev
   ```

2. Проверьте логи - не должно быть ошибок о неправильной конфигурации storage

3. Попробуйте загрузить тестовое изображение через админ-панель:
   - Откройте админ-панель
   - Перейдите в раздел управления меню или акциями
   - Загрузите тестовое изображение
   - Проверьте, что оно появилось в бакете

### 6.2. Проверка в Yandex Cloud Console

1. Откройте бакет `mariko-storage` в Yandex Cloud Console
2. Перейдите на вкладку **"Объекты"** или **"Objects"**
3. Вы должны увидеть загруженные файлы в структуре:
   ```
   menu/
     restaurant-123/
       uuid-image.jpg
   promotions/
     city-1/
       uuid-promo.jpg
   ```

### 6.3. Проверка URL файлов

Если включен публичный доступ:
- Откройте файл в консоли Yandex Cloud
- Скопируйте публичный URL
- Откройте его в браузере - должно отобразиться изображение

---

## Шаг 7: Настройка CORS (опционально)

Если вы планируете загружать файлы напрямую с фронтенда (без backend):

1. Откройте бакет `mariko-storage`
2. Перейдите в **"Настройки"** → **"CORS"**
3. Нажмите **"Настроить CORS"**
4. Добавьте правило:
   - **Разрешенные источники**: 
     - `*` (для разработки)
     - Или конкретные домены: `https://your-app.vercel.app`, `https://your-app.up.railway.app`
   - **Разрешенные методы**: `GET`, `PUT`, `POST`, `DELETE`, `HEAD`
   - **Разрешенные заголовки**: `*`
   - **Максимальный возраст**: `3600` (1 час)
5. Нажмите **"Сохранить"**

**Примечание**: В текущей реализации загрузка идет через backend, поэтому CORS не обязателен.

---

## Шаг 8: Мониторинг и безопасность

### 8.1. Настройка мониторинга

1. В Yandex Cloud Console перейдите в **"Мониторинг"** или **"Monitoring"**
2. Настройте алерты на:
   - Превышение лимита хранилища
   - Необычную активность
   - Превышение бюджета

### 8.2. Рекомендации по безопасности

1. ✅ **Никогда не коммитьте** `.env` файлы с реальными ключами
2. ✅ Используйте **разные ключи** для development и production
3. ✅ Регулярно **ротируйте ключи доступа** (раз в 3-6 месяцев)
4. ✅ Ограничьте права сервисного аккаунта только необходимыми операциями
5. ✅ Используйте **приватные бакеты** для чувствительных данных
6. ✅ Настройте **мониторинг** использования хранилища
7. ✅ Ограничьте доступ по IP (если возможно) в настройках бакета

### 8.3. Ограничение доступа по IP (опционально)

1. Откройте бакет → **"Настройки"** → **"Политика доступа"**
2. Добавьте правило, ограничивающее доступ по IP адресам ваших серверов
3. Это дополнительный уровень безопасности

---

## Шаг 9: Стоимость и лимиты

### 9.1. Тарификация

Yandex Object Storage тарифицируется по:

- **Хранение**: ~0.01₽ за ГБ/месяц (Standard класс)
- **Запросы PUT/POST**: ~0.01₽ за 1000 запросов
- **Запросы GET**: ~0.01₽ за 10000 запросов
- **Исходящий трафик**: зависит от объема (первые 10 ГБ бесплатно)

**Пример расчета для небольшого проекта:**
- 100 изображений по 1 МБ = 100 МБ = 0.1 ГБ
- Хранение: 0.1 ГБ × 0.01₽ = 0.001₽/месяц
- 1000 загрузок: 1000 × 0.01₽ / 1000 = 0.01₽
- **Итого: ~10-50₽/месяц** для небольшого проекта

### 9.2. Лимиты

- Максимальный размер файла: 5 ТБ
- Максимальное количество бакетов: 100 на облако
- Максимальное количество объектов: неограничено

---

## Шаг 10: Troubleshooting

### Проблема: "Access Denied" при загрузке файлов

**Решение:**
1. Проверьте, что сервисный аккаунт имеет роль `storage.editor`
2. Проверьте правильность Access Key ID и Secret Access Key
3. Убедитесь, что переменные окружения установлены правильно

### Проблема: "Bucket not found"

**Решение:**
1. Проверьте правильность имени бакета в переменной `YANDEX_STORAGE_BUCKET_NAME`
2. Убедитесь, что бакет создан в правильном регионе
3. Проверьте, что вы используете правильный endpoint

### Проблема: Файлы не отображаются по публичному URL

**Решение:**
1. Проверьте, включен ли публичный доступ на чтение в настройках бакета
2. Убедитесь, что URL правильный: `https://storage.yandexcloud.net/mariko-storage/path/to/file.jpg`
3. Проверьте права доступа к конкретному файлу

### Проблема: Ошибка "Invalid credentials"

**Решение:**
1. Проверьте, что Access Key ID и Secret Access Key скопированы полностью (без пробелов)
2. Убедитесь, что ключи не истекли (статические ключи не имеют срока действия)
3. Проверьте, что сервисный аккаунт активен

---

## Дополнительные ресурсы

- [Официальная документация Yandex Object Storage](https://cloud.yandex.ru/docs/storage/)
- [S3 API совместимость](https://cloud.yandex.ru/docs/storage/s3/)
- [AWS SDK для JavaScript](https://docs.aws.amazon.com/AWSJavaScriptSDK/v3/latest/clients/client-s3/)
- [Калькулятор стоимости](https://cloud.yandex.ru/prices#storage)

---

## Чеклист готовности

После выполнения всех шагов проверьте:

- [ ] Облако создано в Yandex Cloud
- [ ] Бакет `mariko-storage` создан
- [ ] Сервисный аккаунт `mariko-storage-service` создан
- [ ] Роль `storage.editor` назначена сервисному аккаунту
- [ ] Статический ключ доступа создан и сохранен
- [ ] Переменные окружения настроены локально (`.env.local`)
- [ ] Переменные окружения настроены на Railway
- [ ] Тестовая загрузка файла прошла успешно
- [ ] Файлы видны в консоли Yandex Cloud
- [ ] Публичный доступ настроен (если нужен)

---

## Готово! 🎉

После выполнения всех шагов у вас будет полностью настроенная инфраструктура Yandex Cloud Storage для хранения изображений меню, баннеров акций и других файлов.

Если возникнут вопросы или проблемы - проверьте раздел Troubleshooting или обратитесь к документации Yandex Cloud.
