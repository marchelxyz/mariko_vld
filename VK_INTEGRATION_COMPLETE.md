# Полная интеграция VK Mini Apps

## ✅ Что было сделано

### 1. База данных
- ✅ Добавлено поле `vk_id` в таблицу `user_profiles`
- ✅ Создан индекс для быстрого поиска по VK ID
- ✅ Обновлена миграция БД для автоматического добавления поля

### 2. Бэкенд
- ✅ Создан сервис `vkNotificationService.mjs` для отправки уведомлений через VK API
- ✅ Обновлен `profileService.mjs` для работы с VK ID
- ✅ Обновлен `cartRoutes.mjs` для поддержки VK ID во всех эндпоинтах
- ✅ Добавлена поддержка заголовка `X-VK-Id` в CORS настройках
- ✅ Добавлена отправка уведомлений через VK при создании заказов
- ✅ Обновлена конфигурация для VK (VK_APP_ID, VK_SERVICE_TOKEN, VK_SECRET_KEY)

### 3. Фронтенд
- ✅ Обновлен `VKContext.tsx` для синхронизации профиля с бэкендом
- ✅ Обновлен `cartApi.ts` для передачи VK ID в заголовках и payload
- ✅ Создана утилита `vkHeaders.ts` для работы с заголовками VK

## 📋 Что нужно сделать ВАМ

### Шаг 1: Регистрация мини-приложения на портале VK

1. Перейдите на [https://dev.vk.com](https://dev.vk.com)
2. Войдите в свой аккаунт ВКонтакте
3. Перейдите в раздел **"Мои приложения"** → **"Создать приложение"**
4. Выберите тип: **"Мини-приложение"**
5. Заполните основную информацию:
   - **Название**: Хачапури Марико
   - **Описание**: Приложение для заказа еды из ресторанов сети "Хачапури Марико"
   - **Категория**: Еда и напитки

### Шаг 2: Получение ключей доступа

1. После создания приложения перейдите в **"Настройки"** → **"Разработка"** → **"Ключи доступа"**
2. Скопируйте следующие значения:
   - **App ID** (ID приложения)
   - **Сервисный ключ** (Service Token) - ⚠️ **ВАЖНО**: Этот ключ используется только на сервере!
   - **Защищённый ключ** (Secret Key) - для проверки подписи параметров запуска

### Шаг 3: Настройка переменных окружения

Добавьте следующие переменные в файл `.env` на сервере:

```env
# VK Mini App
VK_APP_ID=ваш_app_id
VK_SERVICE_TOKEN=ваш_сервисный_ключ
VK_SECRET_KEY=ваш_защищённый_ключ
VK_API_VERSION=5.131
```

⚠️ **ВАЖНО**: 
- `VK_SERVICE_TOKEN` используется только на сервере для отправки уведомлений через VK API
- Никогда не передавайте сервисный ключ в клиентский код!
- Храните ключи в безопасном месте

### Шаг 4: Настройка мини-приложения на портале VK

1. Перейдите в **"Настройки"** → **"Мини-приложение"**
2. Укажите:
   - **URL приложения**: `https://vhachapuri.ru` (ваш продакшен домен)
   - **URL для разработки**: `http://localhost:8080` (для локальной разработки)
3. В разделе **"Разрешения"** включите:
   - `messages` - для отправки уведомлений пользователям
4. В разделе **"Безопасность"**:
   - Добавьте доверенные домены: `vhachapuri.ru`, `*.vhachapuri.ru`

### Шаг 5: Включение уведомлений

Для отправки уведомлений через VK API необходимо:

1. В настройках мини-приложения включить **"Уведомления"**
2. Пользователь должен дать согласие на получение уведомлений при первом запуске
3. Убедитесь, что `VK_SERVICE_TOKEN` настроен корректно

### Шаг 6: Тестирование

1. **Локальное тестирование**:
   - Запустите фронтенд: `npm run frontend:dev`
   - Используйте [VK Mini Apps Dev Tools](https://dev.vk.com/mini-apps/dev-tools)
   - Укажите URL для разработки: `http://localhost:8080`

2. **Проверка интеграции**:
   - Откройте приложение через ВКонтакте
   - Создайте тестовый заказ
   - Проверьте, что уведомление пришло в VK

3. **Проверка БД**:
   - Убедитесь, что поле `vk_id` добавлено в таблицу `user_profiles`
   - Проверьте, что профили пользователей сохраняются с VK ID

## 🔧 Технические детали

### Отправка уведомлений

Уведомления отправляются автоматически при:
- Создании нового заказа
- Изменении статуса заказа (если настроено)

Для отправки уведомления используется метод VK API `messages.send`:
```javascript
POST https://api.vk.com/method/messages.send
```

### Идентификация пользователей

Система поддерживает два типа идентификации:
- **Telegram ID** - для пользователей Telegram Mini App
- **VK ID** - для пользователей VK Mini App

Оба типа могут использоваться одновременно для одного пользователя, если он использует оба приложения.

### Заголовки запросов

При работе в VK Mini App автоматически добавляется заголовок:
```
X-VK-Id: <vk_user_id>
```

Это позволяет бэкенду идентифицировать пользователя и отправлять уведомления.

## 📚 Полезные ссылки

- [Документация VK Mini Apps](https://dev.vk.com/mini-apps)
- [VK Bridge API](https://dev.vk.com/mini-apps/development/bridge)
- [VK API - messages.send](https://dev.vk.com/ru/method/messages.send)
- [Портал разработчиков ВК](https://dev.vk.com)

## ⚠️ Важные замечания

1. **Безопасность**: Сервисный ключ должен храниться только на сервере
2. **Лимиты API**: VK API имеет лимиты на количество запросов - соблюдайте их
3. **HTTPS**: Для продакшена обязателен HTTPS
4. **Согласие пользователя**: Пользователь должен дать согласие на получение уведомлений

## 🐛 Решение проблем

### Уведомления не отправляются
1. Проверьте, что `VK_SERVICE_TOKEN` настроен корректно
2. Убедитесь, что пользователь дал согласие на уведомления
3. Проверьте логи сервера на наличие ошибок VK API

### Пользователь не идентифицируется
1. Проверьте, что заголовок `X-VK-Id` передается в запросах
2. Убедитесь, что VK Bridge SDK загружен корректно
3. Проверьте консоль браузера на наличие ошибок

### Ошибки БД
1. Убедитесь, что миграция выполнена успешно
2. Проверьте, что поле `vk_id` существует в таблице `user_profiles`
3. Проверьте логи сервера на наличие ошибок БД
