# Подробный анализ компоновки главной страницы

## Общая структура страницы

Главная страница (`Home.tsx`) состоит из следующих основных секций, расположенных вертикально:

---

## 1. ВЕРХНЯЯ СЕКЦИЯ (Header)

### Расположение
- Контейнер: `div` с классами `bg-transparent pb-5 md:pb-6 relative`
- Внутри: компонент `<Header />`

### Компонент Header
**Файл:** `widgets/header/ui/Header.tsx`

**Элементы:**
1. **Логотип**
   - Изображение: `/images/heroes/hero-image.svg`
   - Размер: `max-w-[clamp(180px,28vw,320px)]` (адаптивный)
   - Расположение: по центру
   - Отступ сверху: `mt-6 md:mt-8`
   - Контейнер: `app-shell app-shell-wide app-hero-space`

2. **Селектор города/ресторана** (AddressCitySelector)
   - Показывается только если `showCitySelector={true}`
   - Отступ сверху: `mt-4`
   - Компонент: `AddressCitySelector`
   - Функционал:
     - Отображает текущий выбранный ресторан в формате "Город, Адрес"
     - При клике открывает выпадающий список всех ресторанов
     - Позволяет выбрать ресторан и отметить его как избранный (звездочка)
     - Стиль: темно-красный фон (`bg-mariko-secondary/80`) с blur-эффектом

---

## 2. СЕКЦИЯ БЫСТРЫХ ДЕЙСТВИЙ (Quick Action Buttons)

### Расположение
- Контейнер: `div` с классами `mt-6 md:mt-8 flex justify-center`
- Внутренний контейнер: `grid` с адаптивными колонками

### Grid-система
- **Мобильные устройства (< 768px):** `grid-cols-4` — 4 кнопки в ряд
- **Планшеты (md, 768px+):** `grid-cols-5` — 5 кнопок (добавляется "Вакансии")
- **Большие экраны (lg, 1024px+):** `grid-cols-5` — 5 кнопок
- **Очень большие экраны (xl, 1280px+):** `grid-cols-4` — 4 кнопки (вакансии переносятся в ServiceCard)

### Кнопки быстрых действий

1. **"Бронь столика"**
   - Иконка: `CalendarDays`
   - Цвет иконки: `text-mariko-primary`
   - Размер иконки: `w-5 h-5 md:w-6 md:h-6`
   - Обработчик: `handleBookingClick()` — проверяет наличие города и ресторана
   - Подсветка при смене города: `highlighted={cityChangedFlash}`

2. **"Заказать доставку"**
   - Иконка: `Truck`
   - Навигация: `/delivery`
   - Подсветка при смене города: `highlighted={cityChangedFlash}`

3. **"Оставить отзыв"**
   - Иконка: `StarIcon`
   - Обработчик: `handleReviewClick()` — открывает внешнюю ссылку или внутреннюю форму
   - Подсветка при смене города: `highlighted={cityChangedFlash}`

4. **"Как нас найти?"**
   - Иконка: `MapPin`
   - Навигация: `/about`
   - Подсветка при смене города: `highlighted={cityChangedFlash}`

5. **"Вакансии"** (условно показывается)
   - Иконка: `Briefcase`
   - Классы видимости: `hidden md:flex xl:hidden` (скрыта на мобильных и очень больших экранах)
   - Обработчик: открывает встроенную страницу или внешнюю ссылку
   - Подсветка при смене города: `highlighted={cityChangedFlash}`

### Стилизация QuickActionButton
- Фон кнопки: белый (`bg-white`)
- Скругление: `rounded-[8px] md:rounded-[12px]`
- Размер: `w-14 md:w-16` (квадратные)
- Тень: `shadow-md`
- Текст под иконкой: белый, размер `text-[11px] md:text-[12px]`
- Анимация подсветки: `animate-city-glow` при смене города

---

## 3. СЕКЦИЯ АКЦИЙ И СЕРВИСОВ (Promotions and Menu/Vacancies)

### Расположение
- Контейнер: `div` с классами `mt-6 md:mt-8`
- Внутренний контейнер: `flex flex-col lg:flex-row` (вертикально на мобильных, горизонтально на больших экранах)

### 3.1. Карусель акций (PromotionsCarousel)

**Расположение:**
- Показывается только если `promotions.length > 0`
- Контейнер: `flex justify-center lg:justify-start`
- Ширина: `w-full max-w-[420px] md:max-w-[520px] lg:max-w-[520px]`
- Отступ снизу на мобильных: `mb-6 lg:mb-0`

**Компонент PromotionsCarousel:**
- Автопрокрутка: каждые 5 секунд (если больше 1 слайда)
- Поддержка свайпов: порог 45px
- Пауза при наведении или перетаскивании
- Элементы управления:
  - Стрелки влево/вправо (если слайдов > 1)
  - Индикаторы точек внизу
- Стиль: стеклянный эффект (`bg-white/10 backdrop-blur-lg`)
- Градиенты: красный и белый blur-эффекты по краям
- При клике на слайд открывается модальное окно с полным описанием акции
- В модальном окне есть кнопка "Забронировать", которая вызывает `onBookTable`

### 3.2. Карточки сервисов (Menu и Vacancies)

**Расположение:**
- Контейнер: `flex justify-center lg:justify-start`
- Grid: `grid-cols-2` на всех размерах экранов
- Максимальная ширина: `max-w-[440px] md:max-w-[480px] lg:max-w-[586px] xl:max-w-[586px]`
- Отступ сверху на больших экранах: `lg:pt-[42px]` (для выравнивания с каруселью)

**Карточка "Меню":**
- Изображение: `/images/services/MENU-CARD.png`
- Соотношение сторон: `aspect-[4/3]`
- Размеры:
  - Мобильные: `max-w-[200px]`
  - Планшеты: `max-w-[230px]`
  - Большие экраны: `lg:h-[220px] lg:w-[293px]`
- Навигация: `/menu`
- Подсветка при смене города: `highlighted={cityChangedFlash}`

**Карточка "Вакансии":**
- Изображение: `/images/services/JOBCARD.png`
- Условная видимость: `block md:hidden xl:block` (скрыта на планшетах и больших экранах, где показывается как QuickActionButton)
- Размеры: аналогичны карточке "Меню"
- Обработчик: открывает встроенную страницу или внешнюю ссылку
- Подсветка при смене города: `highlighted={cityChangedFlash}`

**Стилизация ServiceCard:**
- Фон: белый (`bg-white`)
- Скругление: `rounded-[16px]`
- Тень: `shadow-sm hover:shadow-md`
- Граница: `border border-gray-100`
- Анимация подсветки: `animate-city-glow` при смене города

---

## 4. СЕКЦИЯ РЕКОМЕНДУЕМЫХ БЛЮД (Recommended Section)

### Расположение
- Показывается только если `recommendedDishes.length > 0`
- Контейнер: `div` с классами `mt-10 md:mt-12 -mx-3 md:-mx-6`
- Отрицательные отступы используются для растягивания на всю ширину экрана

### Заголовок секции
- Контейнер: белый фон (`bg-white`), отступы `py-3 md:py-4 px-4 md:px-6`
- Текст: "Рекомендуем попробовать"
- Шрифт: `font-el-messiri text-base md:text-lg font-semibold`
- Иконка справа: `ChevronDown` (декоративная, без функциональности)
- Отступ снизу: `mb-4 md:mb-6`

### Сетка рекомендуемых блюд
- Контейнер: `grid` с адаптивными колонками
- Grid-система:
  - Мобильные (< 640px): `grid-cols-2` — 2 колонки
  - Планшеты (md, 768px+): `grid-cols-3` — 3 колонки
  - Большие экраны (lg, 1024px+): `grid-cols-3` — 3 колонки
  - Очень большие экраны (xl, 1280px+): `grid-cols-4` — 4 колонки
- Отступы между элементами: `gap-2 md:gap-3 lg:gap-4`
- Отступ снизу: `mb-16 md:mb-20`

### Компонент MenuItemComponent

**Варианты отображения:**
- **Мобильный (`variant="mobile"`):** для экранов < 768px
  - Соотношение сторон изображения: `aspect-[4/3]`
  - Размеры текста: `text-[10px] md:text-xs`
- **Компактный (`variant="compact"`):** для экранов >= 768px
  - Соотношение сторон изображения: `aspect-[3/2]`
  - Размеры текста: `text-[11px] md:text-sm`

**Элементы карточки блюда:**
1. **Изображение блюда**
   - Если есть `imageUrl` — показывается изображение
   - Если нет — показывается эмодзи-иконка (определяется по названию блюда)
   - Бейджи в правом верхнем углу:
     - "✨" для новинок (`isNew`)
     - "👑" для рекомендуемых (`isRecommended`)

2. **Информация о блюде**
   - Название: `font-el-messiri font-semibold`, обрезается до 2 строк (`line-clamp-2`)
   - Вес: серый текст (`text-gray-500`)
   - Цена: жирный шрифт (`font-bold`), цвет `text-gray-900`

3. **Маркеры**
   - "🌱" для вегетарианских блюд (`isVegetarian`)
   - "🌶️" для острых блюд (`isSpicy`)

**Обработчик клика:**
- При клике на карточку открывается модальное окно с детальной информацией о блюде

### Модальное окно блюда

**Стиль:**
- Фон: черный с прозрачностью (`bg-black/70 backdrop-blur-sm`)
- Контейнер: стеклянный эффект (`bg-white/12 backdrop-blur-md`)
- Граница: `border border-white/25`
- Скругление: `rounded-[30px]`
- Декоративные элементы:
  - Градиенты для стеклянного эффекта
  - Блик сверху
  - 4 "гвоздика" в углах (декоративные элементы)

**Содержимое:**
- Изображение блюда: `max-h-[40vh] md:max-h-[50vh]`
- Бейджи: "👑 Рекомендуем", "✨ Новинка", "🌱 Вегетарианское", "🌶️ Острое"
- Название: `font-el-messiri text-2xl md:text-3xl font-bold text-white`
- Описание: `text-base md:text-lg text-white/90`
- Цена и вес: `text-2xl md:text-3xl font-bold text-mariko-secondary`

**Закрытие:**
- Клик по фону или по самому модальному окну закрывает его

---

## 5. НИЖНЯЯ НАВИГАЦИЯ (BottomNavigation)

### Расположение
- Компонент: `<BottomNavigation currentPage="home" />`
- Расположение: внизу страницы, фиксированное (`fixed bottom-0`)

### Адаптивное поведение
- **Мобильные/планшеты (< 1100px):**
  - Фиксированная панель внизу экрана
  - Скругление сверху: `rounded-t-2xl`
  - Фон: `bg-black/30 backdrop-blur-3xl`
  - Граница сверху: `border-t border-white/5`
  - Высота учитывается через CSS-переменную `--app-bottom-bar-height`

- **Большие экраны (>= 1100px):**
  - Боковая панель слева (`aside`)
  - Ширина: `160px` (CSS-переменная `--app-rail-width`)
  - Фон: `bg-black/28 backdrop-blur-2xl`
  - Граница справа: `border-r border-white/5`
  - Высота нижней панели устанавливается в `0px`

### Элементы навигации
1. **Главная** (`/`) — активна на этой странице
2. **Франшиза** (`/franchise`)
3. **Профиль** (`/profile`)
4. **Админ-панель** (`/admin`) — показывается только для администраторов

### Стилизация активного элемента
- Фон: `bg-white/10` или `bg-white/15` (в зависимости от режима)
- Текст: белый (`text-white`)
- Тень: `shadow-[0_6px_20px_rgba(0,0,0,0.22)]`

---

## 6. ОБЩИЕ СТИЛИ И ЭФФЕКТЫ

### Фон страницы
- Глобальный фон: прозрачный (`bg-transparent`)
- Фактический фон устанавливается компонентом `RandomBackgroundPattern` (красный цвет `#830E0E`)

### Анимации
- **city-glow:** подсветка элементов при смене города (0.75s)
- **hover эффекты:** масштабирование кнопок (`hover:scale-105`)
- **transition:** плавные переходы для всех интерактивных элементов

### Адаптивность
- Используются Tailwind breakpoints:
  - `md:` — 768px+
  - `lg:` — 1024px+
  - `xl:` — 1280px+
- CSS-переменные для адаптивных размеров:
  - `--app-shell-max: 1200px`
  - `--app-shell-wide: 1320px`
  - `--app-shell-gutter: clamp(16px, 4vw, 28px)`
  - `--app-shell-wide-gutter: clamp(18px, 5vw, 36px)`

---

## НАЙДЕННЫЕ НЕДОЧЕТЫ И ПРОБЛЕМЫ

### 1. ❌ Лишний отступ перед Quick Action Buttons
**Файл:** `Home.tsx`, строка 242
**Проблема:** 
```tsx
<div className="mt-6 md:mt-8 flex justify-center">
```
Дублирование отступов: у родительского контейнера уже есть `space-y-6 md:space-y-8`, а здесь добавляется еще `mt-6 md:mt-8`.

**Рекомендация:** Убрать `mt-6 md:mt-8`, так как отступ уже обеспечивается родительским контейнером.

---

### 2. ❌ Сложная и запутанная логика отображения кнопки "Вакансии"
**Файл:** `Home.tsx`, строки 247, 280-302, 338-364
**Проблема:**
Кнопка "Вакансии" показывается в двух местах с разными условиями:
- Как `QuickActionButton`: `hidden md:flex xl:hidden` (показывается только на md и lg)
- Как `ServiceCard`: `block md:hidden xl:block` (показывается на мобильных и xl+)

Это создает путаницу и сложность в поддержке. Логика разбросана по коду с комментариями.

**Рекомендация:** 
- Вынести логику видимости в отдельную функцию или константу
- Упростить условия или использовать единый компонент для всех размеров экранов

---

### 3. ❌ Декоративная иконка ChevronDown без функциональности
**Файл:** `Home.tsx`, строка 379
**Проблема:**
```tsx
<ChevronDown className="w-5 h-5 md:w-6 md:h-6 text-black" />
```
Иконка `ChevronDown` в заголовке секции "Рекомендуем попробовать" не имеет обработчика клика и не выполняет никакой функции. Это может вводить пользователей в заблуждение.

**Рекомендация:** 
- Либо добавить функциональность (например, прокрутка к секции или развертывание/сворачивание)
- Либо убрать иконку, если она не нужна

---

### 4. ❌ Отрицательные отступы могут вызвать проблемы с прокруткой
**Файл:** `Home.tsx`, строка 373
**Проблема:**
```tsx
<div className="mt-10 md:mt-12 -mx-3 md:-mx-6">
```
Отрицательные горизонтальные отступы (`-mx-3 md:-mx-6`) используются для растягивания секции на всю ширину. Это может вызвать проблемы:
- Горизонтальная прокрутка на некоторых устройствах
- Контент может выходить за границы экрана
- Проблемы с безопасными зонами на мобильных устройствах

**Рекомендация:** 
- Использовать `overflow-x-hidden` на родительском контейнере
- Или использовать другой подход для растягивания (например, `w-screen` с `relative` позиционированием)

---

### 5. ❌ Сложная логика определения количества рекомендуемых блюд
**Файл:** `Home.tsx`, строки 186-194
**Проблема:**
```tsx
let count = 6; // по умолчанию для больших экранов
if (typeof window !== 'undefined') {
  const isTablet = window.matchMedia('(min-width: 768px) and (max-width: 1023px)').matches;
  if (isTablet) {
    count = 3;
  }
}
```
Использование `window.matchMedia` внутри `useEffect` может вызвать проблемы:
- Не синхронизируется с Tailwind breakpoints
- Может не обновляться при изменении размера окна
- Дублирует логику, которая уже есть в CSS Grid

**Рекомендация:** 
- Использовать CSS Grid с `auto-fit` или фиксированным количеством колонок
- Или использовать хук для определения размера экрана
- Упростить логику, убрав проверку планшета (Grid сам адаптируется)

---

### 6. ❌ Несогласованность в использовании `max-w` для карусели акций
**Файл:** `Home.tsx`, строка 312
**Проблема:**
```tsx
<div className="w-full max-w-[420px] md:max-w-[520px] lg:max-w-[520px] mx-auto lg:mx-0">
```
`lg:max-w-[520px]` дублирует `md:max-w-[520px]` без изменений.

**Рекомендация:** Упростить до `max-w-[420px] md:max-w-[520px]`.

---

### 7. ❌ Избыточные комментарии в коде
**Файл:** `Home.tsx`, строки 244-247, 325-327
**Проблема:**
Многострочные комментарии объясняют логику grid-cols, но эта логика уже видна из классов. Комментарии могут устареть при изменении кода.

**Рекомендация:** 
- Либо убрать комментарии, если логика очевидна
- Либо вынести логику в константы с понятными именами

---

### 8. ❌ Жестко заданные размеры для ServiceCard
**Файл:** `Home.tsx`, строки 334, 345
**Проблема:**
```tsx
className="max-w-[200px] md:max-w-[230px] lg:max-w-none lg:h-[220px] lg:w-[293px] w-full [&>div:first-child]:lg:!h-[172px] [&>div:first-child]:lg:!aspect-auto"
```
Сложные классы с переопределением стилей дочерних элементов через `[&>div:first-child]` делают код хрупким и сложным для поддержки.

**Рекомендация:** 
- Вынести эти стили в пропсы компонента `ServiceCard`
- Или использовать CSS-переменные для размеров

---

### 9. ❌ Отсутствие обработки ошибок загрузки изображений в некоторых местах
**Файл:** `Home.tsx`, строки 468-472 (модальное окно блюда)
**Проблема:**
В модальном окне блюда нет обработки ошибок загрузки изображения. Если изображение не загрузится, пользователь увидит битую картинку.

**Рекомендация:** Добавить `onError` обработчик, аналогично тому, как это сделано в `PromotionsCarousel`.

---

### 10. ❌ Потенциальная проблема с производительностью при перемешивании блюд
**Файл:** `Home.tsx`, строка 183
**Проблема:**
```tsx
const shuffled = [...dishes].sort(() => 0.5 - Math.random());
```
Использование `sort(() => 0.5 - Math.random())` не является равномерным распределением и может быть неэффективным для больших массивов.

**Рекомендация:** Использовать алгоритм Фишера-Йетса для равномерного перемешивания:
```tsx
const shuffled = [...dishes];
for (let i = shuffled.length - 1; i > 0; i--) {
  const j = Math.floor(Math.random() * (i + 1));
  [shuffled[i], shuffled[j]] = [shuffled[j], shuffled[i]];
}
```

---

### 11. ❌ Неоптимальная структура условного рендеринга PromotionsCarousel
**Файл:** `Home.tsx`, строки 310-319
**Проблема:**
Карусель акций обернута в несколько вложенных `div` с условным рендерингом. Это усложняет структуру.

**Рекомендация:** Упростить структуру, используя ранний возврат или более простую условную обертку.

---

### 12. ❌ Отсутствие состояния загрузки для PromotionsCarousel
**Файл:** `Home.tsx`
**Проблема:**
Нет индикатора загрузки для акций. Пользователь не видит, что данные загружаются.

**Рекомендация:** Добавить состояние `isLoadingPromotions` и показывать скелетон или спиннер во время загрузки.

---

### 13. ❌ Дублирование логики открытия встроенных страниц
**Файл:** `Home.tsx`, строки 121-128, 286-301, 347-362
**Проблема:**
Функция `openEmbeddedPage` используется для вакансий и отзывов, но логика дублируется в нескольких местах с небольшими вариациями.

**Рекомендация:** Вынести общую логику в отдельную функцию-хелпер или хук.

---

### 14. ❌ Неиспользуемая переменная `modalImageFailed` в PromotionsCarousel
**Файл:** `PromotionsCarousel.tsx`, строки 24, 42-43, 205-216
**Проблема:**
Переменная `modalImageFailed` устанавливается, но не используется для отображения fallback в модальном окне (там всегда показывается "Нет изображения" при ошибке).

**Рекомендация:** Использовать `modalImageFailed` для улучшения UX или убрать, если не нужна.

---

### 15. ❌ Потенциальная проблема с z-index модальных окон
**Файл:** `Home.tsx`, строки 420, 197
**Проблема:**
Оба модальных окна (блюдо и акция) используют `z-[999]`. Если они откроются одновременно, может возникнуть конфликт.

**Рекомендация:** Использовать разные значения z-index или систему управления модальными окнами.

---

## ИТОГОВАЯ ОЦЕНКА

### Сильные стороны:
✅ Хорошая адаптивность на разных размерах экранов
✅ Использование современных паттернов React (хуки, мемоизация)
✅ Красивый UI с анимациями и эффектами
✅ Правильное использование TypeScript
✅ Разделение на компоненты

### Области для улучшения:
⚠️ Упрощение логики условного рендеринга
⚠️ Улучшение обработки ошибок
⚠️ Оптимизация производительности
⚠️ Улучшение доступности (accessibility)
⚠️ Упрощение сложных CSS-классов

---

## РЕКОМЕНДАЦИИ ПО ПРИОРИТЕТАМ

### Высокий приоритет:
1. Исправить логику отображения кнопки "Вакансии" (проблема #2)
2. Добавить обработку ошибок загрузки изображений (проблема #9)
3. Исправить отрицательные отступы (проблема #4)

### Средний приоритет:
4. Упростить логику определения количества блюд (проблема #5)
5. Оптимизировать перемешивание блюд (проблема #10)
6. Добавить состояние загрузки для акций (проблема #12)

### Низкий приоритет:
7. Убрать лишние комментарии (проблема #7)
8. Упростить CSS-классы (проблема #8)
9. Рефакторинг дублирующейся логики (проблема #13)
