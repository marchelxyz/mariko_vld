# Инструкция по подключению PostgreSQL от Яндекс и замене Railway PostgreSQL

## 📋 Обзор

Эта инструкция поможет вам:
1. Создать базу данных PostgreSQL в Yandex Cloud
2. Настроить подключение к базе данных
3. Заменить встроенную БД Railway на PostgreSQL от Яндекс
4. Мигрировать данные (если необходимо)

---

## Шаг 1: Создание PostgreSQL в Yandex Cloud

### 1.1. Открытие консоли Yandex Cloud

1. Перейдите на [https://console.cloud.yandex.ru/](https://console.cloud.yandex.ru/)
2. Войдите через Яндекс ID
3. Выберите нужное облако (Cloud)

### 1.2. Создание кластера PostgreSQL

1. В левом меню найдите раздел **"Managed Service for PostgreSQL"** или **"Управляемая база данных PostgreSQL"**
2. Если это первый раз, нажмите **"Подключить"** или **"Enable"**
3. Нажмите **"Создать кластер"** или **"Create Cluster"**

### 1.3. Настройка кластера

**Основные параметры:**

- **Имя кластера**: `mariko-postgres` (или любое другое)
- **Версия PostgreSQL**: выберите актуальную версию (рекомендуется 15 или 16)
- **Класс хоста**: выберите подходящий класс (например, `s2.micro` для тестирования или `s2.small` для production)
- **Размер диска**: минимум 10 ГБ (рекомендуется 20-50 ГБ для production)
- **Тип диска**: `network-ssd` (быстрее) или `network-hdd` (дешевле)

**База данных:**

- **Имя БД**: `mariko_db` (или другое)
- **Имя пользователя**: `mariko_user` (или другое)
- **Пароль**: создайте надежный пароль (⚠️ **обязательно сохраните его!**)

**Сеть:**

- **Облачная сеть**: выберите существующую или создайте новую
- **Подсеть**: выберите подсеть в нужной зоне доступности (например, `ru-central1-a`)

**Доступ:**

- **Публичный доступ**: включите, если Railway не может подключиться через приватную сеть
- **Список источников**: добавьте IP-адреса Railway (можно оставить пустым для начала и настроить позже)

### 1.4. Создание кластера

1. Проверьте все параметры
2. Нажмите **"Создать кластер"**
3. Дождитесь создания кластера (обычно 5-10 минут)

---

## Шаг 2: Получение строки подключения

### 2.1. Просмотр информации о кластере

1. Откройте созданный кластер `mariko-postgres`
2. Перейдите на вкладку **"Обзор"** или **"Overview"**
3. Найдите раздел **"Подключение"** или **"Connection"**

### 2.2. Формирование строки подключения

Строка подключения имеет следующий формат:

```
postgresql://<username>:<password>@<host>:<port>/<database>
```

**Где взять значения:**

- **username**: имя пользователя, которое вы указали при создании (например, `mariko_user`)
- **password**: пароль, который вы указали при создании
- **host**: FQDN хоста из раздела "Подключение" (например, `c-xxxxx.rw.mdb.yandexcloud.net`)
- **port**: обычно `6432` (порт для SSL) или `5432` (без SSL)
- **database**: имя базы данных (например, `mariko_db`)

**Пример строки подключения:**

```
postgresql://mariko_user:your_password@c-xxxxx.rw.mdb.yandexcloud.net:6432/mariko_db
```

### 2.3. SSL подключение

Yandex Cloud PostgreSQL требует SSL подключение. В коде это уже настроено:

```javascript
ssl: process.env.NODE_ENV === "production" ? { rejectUnauthorized: false } : false
```

Для production используйте порт `6432` и SSL.

---

## Шаг 3: Настройка переменных окружения

### 3.1. Локальная настройка (для разработки)

Создайте или отредактируйте файл `backend/server/.env.local`:

```bash
# PostgreSQL (Yandex Cloud)
DATABASE_URL=postgresql://mariko_user:your_password@c-xxxxx.rw.mdb.yandexcloud.net:6432/mariko_db

# Остальные переменные...
CART_SERVER_PORT=4010
CART_ORDERS_TABLE=cart_orders
ADMIN_TELEGRAM_IDS=577222108
# ... и т.д.
```

**Важно:**
- ⚠️ Не коммитьте файл `.env.local` в Git (он должен быть в `.gitignore`)
- ⚠️ Используйте `.env.local` для локальной разработки
- ⚠️ Используйте Railway Variables для production

### 3.2. Настройка на Railway (для production)

#### Вариант 1: Через Railway CLI (рекомендуется)

```bash
# Установите Railway CLI (если еще не установлен)
npm i -g @railway/cli

# Войдите в Railway
railway login

# Свяжите проект
railway link

# Установите переменную DATABASE_URL для backend сервиса
railway variables set DATABASE_URL="postgresql://mariko_user:your_password@c-xxxxx.rw.mdb.yandexcloud.net:6432/mariko_db" --service backend
```

**Важно:** Обязательно заключите строку подключения в кавычки, если в пароле есть специальные символы!

#### Вариант 2: Через веб-интерфейс Railway

1. Откройте [Railway Dashboard](https://railway.app)
2. Выберите проект → сервис `backend`
3. Перейдите в **Variables**
4. Найдите переменную `DATABASE_URL` (если она была создана автоматически Railway PostgreSQL)
5. Нажмите на неё и замените значение на строку подключения к Yandex Cloud PostgreSQL
6. Или создайте новую переменную `DATABASE_URL` с новым значением

#### Вариант 3: Через скрипт setup-railway-env.sh

1. Обновите файл `backend/server/.env` или `.env.local` с новой строкой подключения:
   ```bash
   DATABASE_URL=postgresql://mariko_user:your_password@c-xxxxx.rw.mdb.yandexcloud.net:6432/mariko_db
   ```

2. Запустите скрипт:
   ```bash
   bash scripts/setup-railway-env.sh
   ```

   Скрипт автоматически прочитает значение из `.env` файла и установит его на Railway.

---

## Шаг 4: Удаление старой БД Railway (опционально)

### 4.1. Проверка использования старой БД

Перед удалением убедитесь, что:
- ✅ Новая БД Yandex Cloud работает корректно
- ✅ Все сервисы используют новую `DATABASE_URL`
- ✅ Данные мигрированы (если необходимо)

### 4.2. Удаление PostgreSQL из Railway

1. Откройте [Railway Dashboard](https://railway.app)
2. Найдите сервис **PostgreSQL** в списке сервисов
3. Откройте его настройки
4. Перейдите в **Settings** → **Danger Zone**
5. Нажмите **Delete** или **Удалить**
6. Подтвердите удаление

**Важно:** 
- ⚠️ После удаления все данные будут потеряны безвозвратно!
- ⚠️ Убедитесь, что вы создали резервную копию, если данные важны
- ⚠️ Убедитесь, что переменная `DATABASE_URL` обновлена на всех сервисах

---

## Шаг 5: Миграция данных (если необходимо)

### 5.1. Создание резервной копии старой БД

Если у вас есть данные в Railway PostgreSQL, создайте резервную копию:

```bash
# Установите PostgreSQL клиент (если еще не установлен)
# На Ubuntu/Debian:
sudo apt-get install postgresql-client

# На macOS:
brew install postgresql

# Создайте дамп базы данных Railway
pg_dump "$OLD_RAILWAY_DATABASE_URL" > backup.sql

# Или через Railway CLI (если Railway предоставляет такую возможность)
railway db dump > backup.sql
```

### 5.2. Восстановление данных в новую БД

```bash
# Восстановите данные в Yandex Cloud PostgreSQL
psql "$YANDEX_DATABASE_URL" < backup.sql
```

### 5.3. Проверка миграции

1. Подключитесь к новой БД и проверьте таблицы:
   ```bash
   psql "$YANDEX_DATABASE_URL" -c "\dt"
   ```

2. Проверьте количество записей в основных таблицах:
   ```bash
   psql "$YANDEX_DATABASE_URL" -c "SELECT COUNT(*) FROM cart_orders;"
   ```

---

## Шаг 6: Инициализация новой БД

### 6.1. Автоматическая инициализация

Если вы используете новую БД без данных, приложение автоматически создаст необходимые таблицы при первом запуске (через `databaseInit.mjs`).

### 6.2. Ручная инициализация

Если нужно запустить инициализацию вручную:

```bash
cd backend/server
node databaseInit.mjs
```

---

## Шаг 7: Настройка доступа по IP (безопасность)

### 7.1. Получение IP-адресов Railway

Railway использует динамические IP-адреса. Для ограничения доступа:

1. В Yandex Cloud Console откройте кластер PostgreSQL
2. Перейдите в **"Безопасность"** или **"Security"**
3. Найдите раздел **"Доступ из интернета"** или **"Public access"**
4. Добавьте IP-адреса или диапазоны Railway

**Альтернатива:** Используйте **"Доступ из облачной сети"** и настройте VPN или приватное подключение (более безопасно, но сложнее).

### 7.2. Рекомендации по безопасности

- ✅ Используйте надежные пароли
- ✅ Ограничьте доступ по IP (если возможно)
- ✅ Регулярно обновляйте пароли
- ✅ Используйте SSL подключение (уже настроено в коде)
- ✅ Не коммитьте строки подключения в Git

---

## Шаг 8: Проверка подключения

### 8.1. Локальная проверка

1. Запустите backend сервер локально:
   ```bash
   cd backend
   npm run dev
   ```

2. Проверьте логи - не должно быть ошибок подключения к БД:
   ```
   ✅ Database connection established
   ✅ Database initialized
   ```

3. Попробуйте выполнить простой запрос через API:
   ```bash
   curl http://localhost:4010/api/cart/orders
   ```

### 8.2. Проверка на Railway

1. Проверьте логи Railway:
   ```bash
   railway logs --service backend
   ```

2. Убедитесь, что нет ошибок подключения:
   ```
   ✅ Database connection established
   ```

3. Проверьте работу API:
   ```bash
   curl https://your-backend.up.railway.app/api/cart/orders
   ```

### 8.3. Проверка в Yandex Cloud Console

1. Откройте кластер PostgreSQL в Yandex Cloud Console
2. Перейдите на вкладку **"Мониторинг"** или **"Monitoring"**
3. Проверьте метрики подключений и запросов
4. Убедитесь, что видны активные подключения

---

## Шаг 9: Обновление других сервисов

### 9.1. Bot сервис

Если bot сервис также использует `DATABASE_URL`, обновите переменную:

```bash
railway variables set DATABASE_URL="postgresql://..." --service bot
```

### 9.2. Проверка всех сервисов

Убедитесь, что все сервисы используют новую БД:

```bash
# Проверьте переменные всех сервисов
railway variables --service backend
railway variables --service bot
```

---

## Шаг 10: Мониторинг и оптимизация

### 10.1. Настройка мониторинга в Yandex Cloud

1. В Yandex Cloud Console перейдите в **"Мониторинг"**
2. Настройте алерты на:
   - Превышение лимита диска
   - Высокую нагрузку CPU
   - Большое количество подключений
   - Ошибки подключения

### 10.2. Резервное копирование

Yandex Cloud автоматически создает резервные копии. Настройте расписание:

1. Откройте кластер PostgreSQL
2. Перейдите в **"Резервные копии"** или **"Backups"**
3. Настройте расписание автоматических бэкапов

### 10.3. Оптимизация производительности

- Регулярно проверяйте медленные запросы
- Используйте индексы для часто запрашиваемых полей
- Мониторьте использование диска и памяти
- При необходимости масштабируйте кластер

---

## Troubleshooting

### Проблема: "Connection refused" или "Connection timeout"

**Решение:**
1. Проверьте, что публичный доступ включен в настройках кластера
2. Проверьте правильность строки подключения (host, port, username, password)
3. Убедитесь, что IP-адрес Railway не заблокирован в настройках безопасности
4. Проверьте, что кластер запущен и доступен

### Проблема: "SSL connection required"

**Решение:**
1. Убедитесь, что используете порт `6432` (SSL) вместо `5432`
2. Проверьте, что в коде включен SSL для production:
   ```javascript
   ssl: process.env.NODE_ENV === "production" ? { rejectUnauthorized: false } : false
   ```

### Проблема: "Authentication failed"

**Решение:**
1. Проверьте правильность username и password
2. Убедитесь, что пароль не содержит специальных символов, которые нужно экранировать в URL
3. Если пароль содержит специальные символы, закодируйте их в URL-формате:
   - `@` → `%40`
   - `:` → `%3A`
   - `/` → `%2F`
   - `#` → `%23`
   - и т.д.

### Проблема: "Database does not exist"

**Решение:**
1. Проверьте правильность имени базы данных в строке подключения
2. Убедитесь, что база данных создана в кластере
3. Проверьте права доступа пользователя к базе данных

### Проблема: Переменная DATABASE_URL не обновляется на Railway

**Решение:**
1. Убедитесь, что вы указали правильный сервис: `--service backend`
2. Проверьте, что переменная установлена: `railway variables --service backend`
3. Перезапустите сервис после обновления переменной:
   ```bash
   railway restart --service backend
   ```

---

## Стоимость и лимиты

### Тарификация Yandex Cloud PostgreSQL

Yandex Cloud PostgreSQL тарифицируется по:
- **Вычислительные ресурсы**: зависит от класса хоста (например, `s2.micro` ~500₽/месяц)
- **Хранилище**: ~3₽ за ГБ/месяц (network-ssd)
- **Резервные копии**: включены в стоимость хранилища

**Пример расчета:**
- Класс хоста: `s2.small` (~1500₽/месяц)
- Хранилище: 20 ГБ × 3₽ = 60₽/месяц
- **Итого: ~1560₽/месяц** для небольшого проекта

### Лимиты

- Максимальный размер БД: зависит от размера диска
- Максимальное количество подключений: зависит от класса хоста
- Максимальный размер транзакции: зависит от конфигурации

---

## Чеклист готовности

После выполнения всех шагов проверьте:

- [ ] Кластер PostgreSQL создан в Yandex Cloud
- [ ] База данных создана в кластере
- [ ] Пользователь создан и имеет необходимые права
- [ ] Строка подключения сформирована и сохранена
- [ ] Переменная `DATABASE_URL` обновлена локально (`.env.local`)
- [ ] Переменная `DATABASE_URL` обновлена на Railway для backend сервиса
- [ ] Переменная `DATABASE_URL` обновлена на Railway для bot сервиса (если используется)
- [ ] Локальное подключение работает (нет ошибок в логах)
- [ ] Railway подключение работает (нет ошибок в логах)
- [ ] Данные мигрированы (если были в старой БД)
- [ ] Старая БД Railway удалена (опционально)
- [ ] Настроен мониторинг в Yandex Cloud
- [ ] Настроено резервное копирование

---

## Дополнительные ресурсы

- [Официальная документация Yandex Cloud PostgreSQL](https://cloud.yandex.ru/docs/managed-postgresql/)
- [Подключение к PostgreSQL](https://cloud.yandex.ru/docs/managed-postgresql/operations/connect)
- [Управление базами данных](https://cloud.yandex.ru/docs/managed-postgresql/operations/databases)
- [Безопасность](https://cloud.yandex.ru/docs/managed-postgresql/operations/security)
- [Мониторинг](https://cloud.yandex.ru/docs/managed-postgresql/operations/monitoring)

---

## Готово! 🎉

После выполнения всех шагов у вас будет полностью настроенная база данных PostgreSQL от Yandex Cloud, которая заменит встроенную БД Railway.

Если возникнут вопросы или проблемы - проверьте раздел Troubleshooting или обратитесь к документации Yandex Cloud.
