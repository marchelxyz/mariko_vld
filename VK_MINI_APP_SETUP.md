# Настройка мини-приложения ВКонтакте

Это руководство поможет вам запустить приложение "Хачапури Марико" как мини-приложение ВКонтакте.

## ✅ Что уже сделано автоматически

- ✅ Подключен VK Bridge SDK в `index.html`
- ✅ Создан контекст `VKContext` для работы с ВК API
- ✅ `VKProvider` интегрирован в приложение
- ✅ Экспортирован хук `useVK()` для использования в компонентах

## 📋 Что нужно сделать вручную

1. Зарегистрироваться на портале разработчиков ВК
2. Создать мини-приложение
3. Настроить URL и параметры приложения
4. Протестировать локально
5. Задеплоить на продакшен
6. Отправить на модерацию

---

## Шаг 1: Регистрация на портале разработчиков ВК

**Нужно зайти на портал для разработчиков ВКонтакте:**

1. Перейдите на [https://dev.vk.com](https://dev.vk.com)
2. Войдите в свой аккаунт ВКонтакте
3. Перейдите в раздел **"Мои приложения"** или **"Создать приложение"**

## Шаг 2: Создание приложения

1. Нажмите кнопку **"Создать приложение"**
2. Выберите тип приложения: **"Мини-приложение"**
3. Заполните основную информацию:
   - **Название приложения**: Хачапури Марико
   - **Описание**: Приложение для заказа еды из ресторанов сети "Хачапури Марико"
   - **Категория**: Еда и напитки
   - **Возрастные ограничения**: 18+ (если требуется)

## Шаг 3: Настройка мини-приложения

После создания приложения перейдите в раздел **"Настройки"** → **"Мини-приложение"**:

### 3.1. Основные настройки

- **URL приложения**: `https://vhachapuri.ru` (или ваш домен, где размещен фронтенд)
- **URL для разработки**: `http://localhost:8080` (для локальной разработки)
- **Разрешения (Scope)**: 
  - `friends` - доступ к списку друзей
  - `email` - доступ к email пользователя (если нужен)
  - `phone_number` - доступ к номеру телефона (если нужен)

### 3.2. Безопасность

- **Секретный ключ**: Сохраните секретный ключ приложения (понадобится для проверки подписи запросов)
- **Доверенные домены**: ⚠️ **ОБЯЗАТЕЛЬНО** добавьте домены, с которых будет загружаться приложение:
  - Если используете домен Vercel (например, `mariko-vld.vercel.app`), добавьте: `mariko-vld.vercel.app`
  - Если используете кастомный домен (например, `vhachapuri.ru`), добавьте:
    - `vhachapuri.ru`
    - `*.vhachapuri.ru` (для поддоменов)
  - ⚠️ **Важно**: Указать URL приложения недостаточно! Нужно отдельно добавить домен в список доверенных доменов в разделе "Безопасность"

### 3.3. Платформа и версии

- **Платформа**: Web
- **Минимальная версия API**: 5.131 (рекомендуется последняя стабильная)

## Шаг 4: Интеграция SDK ВКонтакте

### 4.1. Установка SDK

**Примечание:** В проекте используется CDN версия VK Bridge через `unpkg.com`, поэтому установка npm пакета не требуется. SDK загружается автоматически через скрипт в `index.html`.

Если вы хотите использовать npm пакет вместо CDN (для tree-shaking и оптимизации), выполните:

```bash
cd frontend
npm install @vkontakte/vk-bridge
```

Затем импортируйте его в `main.tsx` или `VKContext.tsx`:

```typescript
import bridge from '@vkontakte/vk-bridge';
```

### 4.2. Подключение SDK в index.html

✅ **Уже выполнено!** Файл `frontend/index.html` уже обновлен и содержит подключение VK SDK:

```html
<!-- VK Mini App SDK -->
<script src="https://unpkg.com/@vkontakte/vk-bridge/dist/browser.min.js"></script>
```

### 4.3. Создание контекста для ВК

✅ **Уже выполнено!** Файл `frontend/src/contexts/VKContext.tsx` создан и содержит:
- Проверку наличия VK Bridge
- Инициализацию VK мини-приложения
- Получение информации о пользователе
- Хук `useVK()` для использования в компонентах

### 4.4. Интеграция в App.tsx

✅ **Уже выполнено!** `VKProvider` добавлен в корневой компонент приложения.

## Шаг 5: Настройка переменных окружения

Добавьте в `.env.example` и `.env.local` (опционально, для серверной проверки подписи):

```env
VITE_VK_APP_ID=ваш_app_id
VITE_VK_SECRET_KEY=ваш_секретный_ключ
```

**Примечание:** Эти переменные нужны только если вы планируете проверять подпись запросов на клиенте или использовать VK API на сервере.

## Шаг 6: Использование VK контекста в компонентах

Теперь вы можете использовать VK контекст в любом компоненте:

```typescript
import { useVK } from '@/contexts';

function MyComponent() {
  const { isVK, user, vkBridge } = useVK();

  if (isVK && user) {
    return (
      <div>
        <p>Привет, {user.first_name} {user.last_name}!</p>
        {user.photo_100 && (
          <img src={user.photo_100} alt="Аватар" />
        )}
      </div>
    );
  }

  return <div>Приложение работает вне ВКонтакте</div>;
}
```

### Пример: Отправка события в ВК

```typescript
import { useVK } from '@/contexts';

function OrderButton() {
  const { vkBridge } = useVK();

  const handleOrder = async () => {
    // Отправка события в ВК для аналитики
    if (vkBridge) {
      await vkBridge.send('VKWebAppEvent', {
        event_name: 'order_placed',
        event_params: {
          order_id: '12345',
          total: 1500
        }
      });
    }
    
    // Ваша логика оформления заказа
  };

  return <button onClick={handleOrder}>Оформить заказ</button>;
}
```

## Шаг 7: Тестирование

### 7.1. Быстрая проверка интеграции

Чтобы проверить, что интеграция работает:

1. Запустите фронтенд: `npm run frontend:dev`
2. Откройте консоль браузера (F12)
3. В консоли выполните:
   ```javascript
   console.log(window.vk); // Должен вывести объект VK Bridge
   ```
4. Если объект существует, интеграция работает корректно

### 7.2. Локальное тестирование в ВК

Для полноценного тестирования в среде ВКонтакте:

1. **Вариант 1: VK Mini Apps Dev Tools**
   - Установите расширение для браузера: [VK Mini Apps Dev Tools](https://chrome.google.com/webstore/detail/vk-mini-apps-dev-tools)
   - Откройте расширение и укажите URL: `http://localhost:8080`
   - Приложение откроется в симуляторе ВК

2. **Вариант 2: Через портал разработчиков**
   - В настройках приложения укажите URL для разработки: `http://localhost:8080`
   - Используйте [VK Mini Apps Dev Tools](https://dev.vk.com/mini-apps/dev-tools) на сайте ВК
   - Откройте приложение через тестовую ссылку

3. **Вариант 3: Прямое тестирование**
   - После настройки приложения на портале ВК
   - Откройте ссылку вида: `https://vk.com/app{ваш_app_id}`
   - Приложение должно открыться в ВКонтакте

### 7.3. Тестирование на продакшене

1. Убедитесь, что фронтенд собран: `npm run frontend:build`
2. Задеплойте на `vhachapuri.ru` (или ваш домен)
3. В настройках приложения ВК укажите URL продакшена: `https://vhachapuri.ru`
4. Откройте приложение через ВКонтакте для проверки
5. Проверьте консоль браузера на наличие ошибок

## Шаг 8: Проверка готовности

Перед публикацией убедитесь, что:

1. ✅ VK SDK подключен в `index.html`
2. ✅ `VKContext` создан и экспортирован
3. ✅ `VKProvider` добавлен в `App.tsx`
4. ✅ Приложение собирается без ошибок: `npm run frontend:build`
5. ✅ Приложение работает локально: `npm run frontend:dev`

## Шаг 9: Публикация

1. Заполните все обязательные поля в настройках приложения
2. Добавьте иконку приложения (512x512px)
3. Добавьте скриншоты для магазина приложений
4. Отправьте приложение на модерацию через раздел **"Публикация"**

## Полезные ссылки

- [Документация VK Mini Apps](https://dev.vk.com/mini-apps)
- [VK Bridge API](https://dev.vk.com/mini-apps/development/bridge)
- [Примеры мини-приложений](https://github.com/VKCOM/vk-mini-apps)
- [Портал разработчиков ВК](https://dev.vk.com)

## Дополнительные настройки

### Настройка темы

ВК мини-приложения поддерживают светлую и темную темы. Можно использовать:

```typescript
const theme = await vk.Bridge.send('VKWebAppGetTheme');
// theme.scheme будет 'bright' или 'dark'
```

### Обработка платежей

Если нужно интегрировать платежи ВК:

1. Включите платежи в настройках приложения
2. Используйте `VKWebAppOpenPayForm` для открытия формы оплаты
3. Обрабатывайте события платежей через `VKWebAppPaymentResult`

### Уведомления

Для отправки уведомлений пользователям:

1. Включите уведомления в настройках приложения
2. Используйте VK API для отправки уведомлений через сервер

## Важные замечания

1. **HTTPS обязателен** для продакшена (ВК требует HTTPS)
2. **CORS настройки**: Убедитесь, что ваш бэкенд разрешает запросы с домена ВК
3. **Проверка подписи**: Всегда проверяйте подпись запросов от ВК на сервере
4. **Лимиты API**: Соблюдайте лимиты запросов к VK API
