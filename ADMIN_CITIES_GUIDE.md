# 🏙️ Управление городами - Краткая инструкция

## 🎯 Быстрый старт

### Активировать/Деактивировать город

1. Откройте **Админ-панель** → **Управление ресторанами**
2. Найдите нужный город
3. Нажмите кнопку 👁️ (активировать) или 👁️‍🗨️ (деактивировать)
4. Откройте файл `src/shared/config/activeCities.ts`
5. Обновите массив `ACTIVE_CITY_IDS`:

```typescript
export const ACTIVE_CITY_IDS: string[] = [
  "zhukovsky",  // ✅ Жуковский - активен
  "kaluga",     // ✅ Калуга - активна
  "penza",      // ✅ Пенза - активна
  // Добавьте ID города чтобы активировать его
  // Удалите ID города чтобы скрыть его
];
```

6. Сохраните файл
7. Соберите проект: `npm run build`
8. Загрузите на сервер TimeWeb

---

## 📍 Где находятся города

### Пользователи видят активные города:
- **Главная страница** → Выбор города (вверху)
- **Рестораны** → Список городов
- **Доставка** → Выбор адреса

### Админ видит ВСЕ города:
- **Админ-панель** → Управление ресторанами (все города, включая деактивированные)

---

## ✅ Текущие активные города

Сейчас активны **3 города**:
1. **Жуковский** (`zhukovsky`)
2. **Калуга** (`kaluga`)
3. **Пенза** (`penza`)

Все остальные города **скрыты** от пользователей.

---

## 🚀 Применение изменений

### Локальная разработка (`npm run dev`)
Изменения применяются сразу после сохранения файла

### Продакшен (TimeWeb сервер)
1. Измените `activeCities.ts`
2. `npm run build` - сборка проекта
3. Загрузите `dist/` на сервер
4. Пользователи увидят изменения после обновления страницы

---

## 📋 Список всех доступных городов

| Город | ID | Статус |
|-------|-----|--------|
| Жуковский | `zhukovsky` | ✅ Активен |
| Калуга | `kaluga` | ✅ Активен |
| Пенза | `penza` | ✅ Активен |
| Нижний Новгород | `nizhny-novgorod` | ❌ Скрыт |
| Санкт-Петербург | `saint-petersburg` | ❌ Скрыт |
| Казань | `kazan` | ❌ Скрыт |
| Кемерово | `kemerovo` | ❌ Скрыт |
| Томск | `tomsk` | ❌ Скрыт |
| Смоленск | `smolensk` | ❌ Скрыт |
| Самара | `samara` | ❌ Скрыт |
| Новосибирск | `novosibirsk` | ❌ Скрыт |
| Магнитогорск | `magnitogorsk` | ❌ Скрыт |
| Балахна | `balakhna` | ❌ Скрыт |
| Кстово | `kstovo` | ❌ Скрыт |
| Лесной Городок | `lesnoy-gorodok` | ❌ Скрыт |
| Новороссийск | `novorossiysk` | ❌ Скрыт |
| Одинцово | `odintsovo` | ❌ Скрыт |
| Нефтекамск | `neftekamsk` | ❌ Скрыт |
| Астана | `astana` | ❌ Скрыт |
| Атырау | `atyrau` | ❌ Скрыт |
| Волгоград | `volgograd` | ❌ Скрыт |
| Бугульма | `bugulma` | ❌ Скрыт |
| Уфа | `ufa` | ❌ Скрыт |
| Саранск | `saransk` | ❌ Скрыт |

---

## 💡 Примеры

### Активировать Санкт-Петербург

```typescript
export const ACTIVE_CITY_IDS: string[] = [
  "zhukovsky",
  "kaluga",
  "penza",
  "saint-petersburg",  // ← Добавили
];
```

### Деактивировать Пензу

```typescript
export const ACTIVE_CITY_IDS: string[] = [
  "zhukovsky",
  "kaluga",
  // "penza",  // ← Удалили или закомментировали
];
```

### Активировать все города

```typescript
export const USE_ACTIVE_CITIES_FILTER = false;  // Отключить фильтр
```

---

## ⚠️ Важно!

### Изменения НЕ применяются автоматически
После редактирования `activeCities.ts` нужно:
1. Собрать проект (`npm run build`)
2. Загрузить на сервер

### Кэш браузера
Пользователи могут не увидеть изменения сразу:
- Попросите обновить страницу (Ctrl+F5)
- Или очистить кэш Telegram Mini App

### Проверка
Откройте консоль (F12) и выполните:
```javascript
import { getAvailableCities } from '@/shared/data/cities';
console.log(getAvailableCities());  // Покажет только активные города
```

---

## 📚 Дополнительная документация

- **Полная инструкция**: `docs/DEPLOY_CITY_CHANGES.md`
- **Админ-панель**: `docs/ADMIN_PANEL.md`
- **Быстрый старт**: `ADMIN_QUICK_START.md`

---

**Версия**: 1.0  
**Дата**: Ноябрь 2025

