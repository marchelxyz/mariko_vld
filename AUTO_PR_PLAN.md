# 🚀 План автоматизации создания Pull Request

## Цель
Автоматически создавать Pull Request после завершения каждой задачи с изменениями кода.

## Анализ текущей ситуации

### ✅ Что уже есть:
1. Правило `.cursor/rules/auto_pr.md` - указывает мне автоматически создавать PR
2. Скрипт `create_pr_universal.sh` - универсальный скрипт для создания PR
3. Скрипт `create_pr_fixed.ps1` - PowerShell версия с исправленной кодировкой

### ❌ Проблемы:
1. PowerShell имеет проблемы с кириллицей в путях
2. Нужно убедиться, что скрипт вызывается автоматически после каждой задачи
3. Нужно автоматически определять название ветки и PR из контекста задачи

## 🎯 РЕШЕНИЕ: Использовать Git Bash скрипт

### Почему Git Bash:
- ✅ Надежно работает с кириллицей
- ✅ Cursor может вызывать bash скрипты через `run_terminal_cmd`
- ✅ Кросс-платформенное решение
- ✅ Не требует настройки профиля PowerShell

## 📋 План реализации

### Шаг 1: Улучшить универсальный скрипт
- ✅ Скрипт уже существует и работает
- ✅ Автоматически определяет ветку и название PR
- ✅ Обрабатывает все случаи (коммит, ветка, push, PR)

### Шаг 2: Обновить правило автоматизации
- ✅ Правило уже существует в `.cursor/rules/auto_pr.md`
- ✅ Нужно убедиться, что оно указывает использовать скрипт

### Шаг 3: Создать умный скрипт-обертку
Создать скрипт, который:
- Автоматически определяет название ветки из изменений
- Генерирует название PR из коммитов или описания задачи
- Работает в любом терминале (Git Bash, PowerShell через bash)

### Шаг 4: Протестировать автоматизацию
- Проверить, что скрипт вызывается автоматически
- Убедиться, что PR создается корректно

## 🔧 Техническая реализация

### Вариант 1: Использовать существующий скрипт (РЕКОМЕНДУЕТСЯ)

**Как это работает:**
1. После завершения задачи я автоматически вызываю:
   ```bash
   bash create_pr_universal.sh feat/branch-name "feat: описание"
   ```
2. Скрипт автоматически:
   - Проверяет изменения
   - Коммитит их
   - Создает/переключается на ветку
   - Отправляет на GitHub
   - Создает PR

**Преимущества:**
- ✅ Работает через Git Bash (надежно с кириллицей)
- ✅ Cursor может вызвать через `run_terminal_cmd`
- ✅ Уже протестирован и работает

### Вариант 2: Создать улучшенный скрипт с автоопределением

Создать скрипт `auto_pr.sh`, который:
- Анализирует git diff для определения типа изменений
- Автоматически генерирует название ветки и PR
- Использует Conventional Commits формат

## 📝 Обновленное правило для Cursor

Я буду автоматически выполнять после завершения задачи:

```bash
# Определяю название ветки из задачи
BRANCH_NAME="feat/описание-задачи"

# Определяю название PR
PR_TITLE="feat: описание задачи"

# Вызываю скрипт
bash create_pr_universal.sh "$BRANCH_NAME" "$PR_TITLE"
```

## ✅ Финальный план действий

1. **Убедиться, что скрипт работает** ✅ (уже работает)
2. **Обновить правило** - указать использовать bash скрипт
3. **Протестировать** - выполнить тестовую задачу и проверить создание PR
4. **Документировать** - создать инструкцию для пользователя

## 🎯 Рекомендация

**Использовать Git Bash скрипт `create_pr_universal.sh`** - это самое надежное решение:
- Работает с кириллицей
- Может быть вызван из Cursor
- Уже протестирован
- Универсальный (работает в любом репозитории)

## 📋 Чеклист для автоматизации

- [x] Скрипт создан и работает
- [x] Правило в `.cursor/rules/auto_pr.md` обновлено
- [ ] Протестировать автоматическое создание PR
- [ ] Убедиться, что скрипт вызывается после каждой задачи
