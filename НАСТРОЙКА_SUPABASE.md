# 🗄️ НАСТРОЙКА SUPABASE - Пошаговая инструкция

## ✅ Что получите после настройки:

- 🚀 **Моментальные изменения** для всех пользователей
- ⚡ **Нажали кнопку в админке** → Город сразу исчез у всех!
- 🔄 **Real-time обновления** - без перезагрузки страницы
- 💾 **Всё в базе данных** - надежно и масштабируемо

---

## 📋 Пошаговая настройка (5 минут)

### Шаг 1: Откройте Supabase Dashboard

Ссылка: https://supabase.com/dashboard/project/gdcqndpfkngtaargxcve

### Шаг 2: Откройте SQL Editor

1. В левом меню: **Database**
2. Нажмите **SQL Editor**
3. Нажмите **New query**

### Шаг 3: Скопируйте SQL-код

Откройте файл **`supabase_setup.sql`** в корне проекта и скопируйте **весь** код

### Шаг 4: Вставьте и выполните

1. Вставьте код в SQL Editor
2. Нажмите **RUN** (или Ctrl+Enter)
3. Должны увидеть: "Success. No rows returned"

### Шаг 5: Включите Realtime

1. Перейдите в **Database** → **Replication**
2. Найдите таблицы:
   - `cities` → Включите переключатель
   - `restaurants` → Включите переключатель
3. Должен быть зеленый статус "Realtime enabled"

### Шаг 6: Проверьте данные

В SQL Editor выполните:

```sql
SELECT * FROM cities WHERE is_active = true;
```

Должны увидеть 3 города:
- Жуковский
- Калуга
- Пенза

---

## 🎉 Готово! Проверка работы

### 1. Перезапустите приложение

```bash
npm run dev
```

### 2. Откройте админ-панель

Нажмите 🛡️ **Админ-панель** → **Управление ресторанами**

### 3. Проверьте индикатор

Должна быть **зеленая панель**:
```
✅ Supabase подключен - Real-time режим
Изменения применяются моментально для ВСЕХ пользователей!
```

Если видите синюю панель "⚠️ Режим файла конфигурации" - проверьте:
- Выполнены ли SQL-запросы в Supabase
- Есть ли ошибки в консоли (F12)

### 4. Протестируйте изменения

1. Откройте приложение в **двух вкладках**:
   - Вкладка 1: Админ-панель
   - Вкладка 2: Главная страница

2. В админке деактивируйте город (например, Жуковский)

3. На главной странице город должен **исчезнуть МГНОВЕННО** (без обновления!)

---

## ✨ Преимущества

### Было (без Supabase):
```
Деактивировал город → 
Открыл activeCities.ts → 
Удалил ID → 
npm run build → 
Загрузил на сервер → 
Пользователи обновили страницу → 
Увидели изменения
⏱️ ~10-30 минут
```

### Стало (с Supabase):
```
Нажал кнопку в админке → 
Все увидели изменения мгновенно!
⏱️ ~1 секунда ⚡
```

---

## 🎯 Что теперь можно делать

### В админ-панели:

1. **Активация/Деактивация городов**
   - Нажали кнопку → Готово!
   - Все пользователи видят изменения сразу
   - Не нужно деплоить

2. **Скрытие ресторанов**
   - Можно скрыть конкретные адреса
   - Город останется, но без некоторых ресторанов

3. **Добавление новых городов**
   - Добавили через админку
   - Сразу появился у всех пользователей

---

## ⚙️ Настройки

### Файл `.env` (или `env.`)

Должен содержать:
```env
VITE_SUPABASE_URL=https://gdcqndpfkngtaargxcve.supabase.co
VITE_SUPABASE_ANON_KEY=ваш_ключ
```

У вас это уже настроено! ✅

---

## 🔍 Проверка в Supabase

### Посмотреть активные города:

```sql
SELECT * FROM cities WHERE is_active = true ORDER BY display_order;
```

### Активировать город вручную (через SQL):

```sql
UPDATE cities SET is_active = true WHERE id = 'nizhny-novgorod';
```

### Деактивировать город:

```sql
UPDATE cities SET is_active = false WHERE id = 'zhukovsky';
```

### Посмотреть рестораны города:

```sql
SELECT * FROM restaurants WHERE city_id = 'zhukovsky';
```

---

## 🚀 Деплой на TimeWeb

После настройки Supabase на локальной машине:

1. Соберите проект:
```bash
npm run build
```

2. Загрузите `dist/` на TimeWeb сервер

3. **Важно!** Убедитесь, что на сервере тоже есть файл `.env` с ключами Supabase

---

## 📱 Для пользователей

### Что увидят пользователи:

1. **Мгновенные обновления**
   - Деактивировали город → Исчез сразу
   - Активировали город → Появился сразу

2. **Без обновления страницы**
   - Real-time технология
   - Автоматическая синхронизация

3. **Всегда актуальные данные**
   - Нет устаревшего кэша
   - Нет задержек

---

## ❓ FAQ

### Нужно ли теперь редактировать activeCities.ts?

**Нет!** После настройки Supabase:
- Всё управление через админ-панель
- Файл `activeCities.ts` используется только как fallback

### Что если Supabase недоступен?

Приложение автоматически переключится на файл `activeCities.ts`:
- Синяя панель в админке
- Работает как раньше (через деплой)

### Безопасно ли это?

✅ Да! У вас настроен Row Level Security  
✅ Только авторизованные могут менять данные  
✅ Все читают через безопасный API  

---

## 🎊 Поздравляем!

После настройки вы получаете:
- ✅ Real-time управление городами
- ✅ Моментальные изменения для всех
- ✅ Удобную админ-панель
- ✅ Масштабируемое решение

**Настройте один раз и наслаждайтесь удобством! 🚀**

