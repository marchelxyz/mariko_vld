# Инструкция по подключению PostgreSQL от VK Cloud

## 📋 Обзор

Эта инструкция поможет вам:
1. Создать базу данных PostgreSQL в VK Cloud
2. Настроить подключение к базе данных
3. Настроить переменные окружения для приложения
4. Проверить подключение и инициализировать БД

---

## Шаг 1: Создание PostgreSQL в VK Cloud

### 1.1. Открытие консоли VK Cloud

1. Перейдите на [https://mcs.mail.ru/](https://mcs.mail.ru/) или [https://cloud.vk.com/](https://cloud.vk.com/)
2. Войдите через VK ID или Mail.ru аккаунт
3. Выберите нужный проект

### 1.2. Создание инстанса PostgreSQL

1. В левом меню найдите раздел **"Базы данных"** или **"Databases"**
2. Выберите **"PostgreSQL"**
3. Нажмите **"Создать инстанс"** или **"Create Instance"**

### 1.3. Настройка инстанса

**Основные параметры:**

- **Имя инстанса**: `mariko-postgres` (или любое другое)
- **Версия PostgreSQL**: выберите актуальную версию (рекомендуется 15 или 16)
- **Конфигурация**: выберите подходящую конфигурацию
  - Для тестирования: минимальная конфигурация (1 vCPU, 2 GB RAM)
  - Для production: рекомендуется минимум 2 vCPU, 4 GB RAM
- **Размер диска**: минимум 20 ГБ (рекомендуется 50-100 ГБ для production)
- **Тип диска**: `SSD` (быстрее) или `HDD` (дешевле)

**База данных:**

- **Имя БД**: `mariko_db` (или другое)
- **Имя пользователя**: `mariko_user` (или другое)
- **Пароль**: создайте надежный пароль (⚠️ **обязательно сохраните его!**)

**Сеть:**

- **Сеть**: выберите существующую сеть или создайте новую
- **Подсеть**: выберите подсеть в нужной зоне доступности

**Доступ:**

- **Публичный доступ**: включите, если приложение не может подключиться через приватную сеть
- **Белый список IP**: добавьте IP-адреса вашего приложения (можно настроить позже)

### 1.4. Создание инстанса

1. Проверьте все параметры
2. Нажмите **"Создать"** или **"Create"**
3. Дождитесь создания инстанса (обычно 5-10 минут)

---

## Шаг 2: Получение строки подключения

### 2.1. Просмотр информации об инстансе

1. Откройте созданный инстанс `mariko-postgres`
2. Перейдите на вкладку **"Подключение"** или **"Connection"**
3. Найдите раздел с параметрами подключения

### 2.2. Формирование строки подключения

Строка подключения имеет следующий формат:

```
postgresql://<username>:<password>@<host>:<port>/<database>
```

**Где взять значения:**

- **username**: имя пользователя, которое вы указали при создании (например, `mariko_user`)
- **password**: пароль, который вы указали при создании
- **host**: FQDN хоста из раздела "Подключение" (например, `postgres-xxxxx.vk.cloud` или `xxxxx.mcs.mail.ru`)
- **port**: обычно `5432` (стандартный порт PostgreSQL) или `6432` (если используется SSL через специальный порт)
- **database**: имя базы данных (например, `mariko_db`)

**Пример строки подключения:**

```
postgresql://mariko_user:your_password@postgres-xxxxx.vk.cloud:5432/mariko_db
```

### 2.3. SSL подключение

VK Cloud PostgreSQL поддерживает SSL подключение. В коде это уже настроено:

```javascript
ssl: process.env.NODE_ENV === "production" ? { rejectUnauthorized: false } : false
```

Для production рекомендуется использовать SSL. Если VK Cloud требует SSL, используйте параметр `?sslmode=require` в строке подключения или настройте SSL в коде.

---

## Шаг 3: Настройка переменных окружения

### 3.1. Локальная настройка (для разработки)

Создайте или отредактируйте файл `backend/server/.env.local`:

```bash
# PostgreSQL (VK Cloud)
DATABASE_URL=postgresql://mariko_user:your_password@postgres-xxxxx.vk.cloud:5432/mariko_db

# Остальные переменные...
CART_SERVER_PORT=4010
CART_ORDERS_TABLE=cart_orders
ADMIN_TELEGRAM_IDS=577222108
# ... и т.д.
```

**Важно:**
- ⚠️ Не коммитьте файл `.env.local` в Git (он должен быть в `.gitignore`)
- ⚠️ Используйте `.env.local` для локальной разработки
- ⚠️ Используйте переменные окружения вашей платформы развертывания для production

### 3.2. Настройка на Railway (для production)

#### Вариант 1: Через Railway CLI (рекомендуется)

```bash
# Установите Railway CLI (если еще не установлен)
npm i -g @railway/cli

# Войдите в Railway
railway login

# Свяжите проект
railway link

# Установите переменную DATABASE_URL для backend сервиса
railway variables set DATABASE_URL="postgresql://mariko_user:your_password@postgres-xxxxx.vk.cloud:5432/mariko_db" --service backend
```

**Важно:** Обязательно заключите строку подключения в кавычки, если в пароле есть специальные символы!

#### Вариант 2: Через веб-интерфейс Railway

1. Откройте [Railway Dashboard](https://railway.app)
2. Выберите проект → сервис `backend`
3. Перейдите в **Variables**
4. Найдите переменную `DATABASE_URL` (если она была создана автоматически Railway PostgreSQL)
5. Нажмите на неё и замените значение на строку подключения к VK Cloud PostgreSQL
6. Или создайте новую переменную `DATABASE_URL` с новым значением

#### Вариант 3: Через скрипт setup-railway-env.sh

1. Обновите файл `backend/server/.env` или `.env.local` с новой строкой подключения:
   ```bash
   DATABASE_URL=postgresql://mariko_user:your_password@postgres-xxxxx.vk.cloud:5432/mariko_db
   ```

2. Запустите скрипт:
   ```bash
   bash scripts/setup-railway-env.sh
   ```

   Скрипт автоматически прочитает значение из `.env` файла и установит его на Railway.

---

## Шаг 4: Настройка доступа по IP (безопасность)

### 4.1. Получение IP-адресов приложения

Если ваше приложение развернуто на Railway или другой платформе, вам нужно добавить IP-адреса в белый список VK Cloud.

1. В VK Cloud Console откройте инстанс PostgreSQL
2. Перейдите в **"Безопасность"** или **"Security"**
3. Найдите раздел **"Белый список IP"** или **"IP Whitelist"**
4. Добавьте IP-адреса или диапазоны вашего приложения

**Для Railway:**
- Railway использует динамические IP-адреса
- Можно временно разрешить доступ со всех IP (`0.0.0.0/0`) для тестирования, но это небезопасно для production
- Рекомендуется использовать VPN или приватное подключение, если доступно

### 4.2. Рекомендации по безопасности

- ✅ Используйте надежные пароли
- ✅ Ограничьте доступ по IP (если возможно)
- ✅ Регулярно обновляйте пароли
- ✅ Используйте SSL подключение (уже настроено в коде)
- ✅ Не коммитьте строки подключения в Git
- ✅ Используйте приватные сети, если доступно

---

## Шаг 5: Инициализация базы данных

### 5.1. Автоматическая инициализация

Приложение автоматически создаст необходимые таблицы при первом запуске (через `databaseInit.mjs`).

### 5.2. Ручная инициализация

Если нужно запустить инициализацию вручную:

```bash
cd backend/server
node databaseInit.mjs
```

---

## Шаг 6: Проверка подключения

### 6.1. Локальная проверка

1. Запустите backend сервер локально:
   ```bash
   cd backend
   npm run dev
   ```

2. Проверьте логи - не должно быть ошибок подключения к БД:
   ```
   ✅ Database connection established
   ✅ Database initialized
   ```

3. Попробуйте выполнить простой запрос через API:
   ```bash
   curl http://localhost:4010/api/cart/orders
   ```

### 6.2. Проверка на Railway

1. Проверьте логи Railway:
   ```bash
   railway logs --service backend
   ```

2. Убедитесь, что нет ошибок подключения:
   ```
   ✅ Database connection established
   ```

3. Проверьте работу API:
   ```bash
   curl https://your-backend.up.railway.app/api/cart/orders
   ```

### 6.3. Проверка в VK Cloud Console

1. Откройте инстанс PostgreSQL в VK Cloud Console
2. Перейдите на вкладку **"Мониторинг"** или **"Monitoring"**
3. Проверьте метрики подключений и запросов
4. Убедитесь, что видны активные подключения

---

## Шаг 7: Обновление других сервисов

### 7.1. Bot сервис

Если bot сервис также использует `DATABASE_URL`, обновите переменную:

```bash
railway variables set DATABASE_URL="postgresql://..." --service bot
```

### 7.2. Проверка всех сервисов

Убедитесь, что все сервисы используют новую БД:

```bash
# Проверьте переменные всех сервисов
railway variables --service backend
railway variables --service bot
```

---

## Шаг 8: Мониторинг и оптимизация

### 8.1. Настройка мониторинга в VK Cloud

1. В VK Cloud Console перейдите в **"Мониторинг"**
2. Настройте алерты на:
   - Превышение лимита диска
   - Высокую нагрузку CPU
   - Большое количество подключений
   - Ошибки подключения

### 8.2. Резервное копирование

VK Cloud автоматически создает резервные копии. Настройте расписание:

1. Откройте инстанс PostgreSQL
2. Перейдите в **"Резервные копии"** или **"Backups"**
3. Настройте расписание автоматических бэкапов

### 8.3. Оптимизация производительности

- Регулярно проверяйте медленные запросы
- Используйте индексы для часто запрашиваемых полей
- Мониторьте использование диска и памяти
- При необходимости масштабируйте инстанс

---

## Troubleshooting

### Проблема: "Connection refused" или "Connection timeout"

**Решение:**
1. Проверьте, что публичный доступ включен в настройках инстанса
2. Проверьте правильность строки подключения (host, port, username, password)
3. Убедитесь, что IP-адрес приложения не заблокирован в белом списке
4. Проверьте, что инстанс запущен и доступен
5. Проверьте настройки файрвола VK Cloud

### Проблема: "SSL connection required"

**Решение:**
1. Убедитесь, что используете правильный порт (обычно `5432`)
2. Проверьте, что в коде включен SSL для production:
   ```javascript
   ssl: process.env.NODE_ENV === "production" ? { rejectUnauthorized: false } : false
   ```
3. Если VK Cloud требует SSL, добавьте `?sslmode=require` в строку подключения:
   ```
   postgresql://user:password@host:port/database?sslmode=require
   ```

### Проблема: "Authentication failed"

**Решение:**
1. Проверьте правильность username и password
2. Убедитесь, что пароль не содержит специальных символов, которые нужно экранировать в URL
3. Если пароль содержит специальные символы, закодируйте их в URL-формате:
   - `@` → `%40`
   - `:` → `%3A`
   - `/` → `%2F`
   - `#` → `%23`
   - `%` → `%25`
   - и т.д.

### Проблема: "Database does not exist"

**Решение:**
1. Проверьте правильность имени базы данных в строке подключения
2. Убедитесь, что база данных создана в инстансе
3. Проверьте права доступа пользователя к базе данных

### Проблема: "Too many connections"

**Решение:**
1. Проверьте количество активных подключений в мониторинге VK Cloud
2. Убедитесь, что пул подключений настроен правильно в коде
3. Рассмотрите возможность увеличения лимита подключений или масштабирования инстанса

### Проблема: Переменная DATABASE_URL не обновляется на Railway

**Решение:**
1. Убедитесь, что вы указали правильный сервис: `--service backend`
2. Проверьте, что переменная установлена: `railway variables --service backend`
3. Перезапустите сервис после обновления переменной:
   ```bash
   railway restart --service backend
   ```

---

## Стоимость и лимиты

### Тарификация VK Cloud PostgreSQL

VK Cloud PostgreSQL тарифицируется по:
- **Вычислительные ресурсы**: зависит от конфигурации (например, 1 vCPU, 2 GB RAM ~500-1000₽/месяц)
- **Хранилище**: зависит от типа диска (SSD дороже HDD)
- **Резервные копии**: обычно включены в стоимость хранилища

**Пример расчета:**
- Конфигурация: 2 vCPU, 4 GB RAM (~1500-2000₽/месяц)
- Хранилище: 50 ГБ SSD (~500-1000₽/месяц)
- **Итого: ~2000-3000₽/месяц** для небольшого проекта

### Лимиты

- Максимальный размер БД: зависит от размера диска
- Максимальное количество подключений: зависит от конфигурации
- Максимальный размер транзакции: зависит от конфигурации

---

## Чеклист готовности

После выполнения всех шагов проверьте:

- [ ] Инстанс PostgreSQL создан в VK Cloud
- [ ] База данных создана в инстансе
- [ ] Пользователь создан и имеет необходимые права
- [ ] Строка подключения сформирована и сохранена
- [ ] Переменная `DATABASE_URL` обновлена локально (`.env.local`)
- [ ] Переменная `DATABASE_URL` обновлена на Railway для backend сервиса
- [ ] Переменная `DATABASE_URL` обновлена на Railway для bot сервиса (если используется)
- [ ] IP-адреса добавлены в белый список VK Cloud
- [ ] Локальное подключение работает (нет ошибок в логах)
- [ ] Railway подключение работает (нет ошибок в логах)
- [ ] База данных инициализирована (таблицы созданы)
- [ ] Настроен мониторинг в VK Cloud
- [ ] Настроено резервное копирование

---

## Дополнительные ресурсы

- [Официальная документация VK Cloud](https://mcs.mail.ru/docs/)
- [Документация по PostgreSQL в VK Cloud](https://mcs.mail.ru/docs/ru/dbs/databases/postgresql/)
- [Подключение к PostgreSQL](https://mcs.mail.ru/docs/ru/dbs/databases/postgresql/connect/)
- [Безопасность](https://mcs.mail.ru/docs/ru/dbs/databases/postgresql/security/)
- [Мониторинг](https://mcs.mail.ru/docs/ru/dbs/databases/postgresql/monitoring/)

---

## Готово! 🎉

После выполнения всех шагов у вас будет полностью настроенная база данных PostgreSQL от VK Cloud.

Если возникнут вопросы или проблемы - проверьте раздел Troubleshooting или обратитесь к документации VK Cloud.
