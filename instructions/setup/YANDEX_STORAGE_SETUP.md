# Инструкция по подключению Yandex Object Storage

## 📋 Содержание
1. [Создание бакета в Yandex Object Storage](#1-создание-бакета)
2. [Получение ключей доступа](#2-получение-ключей-доступа)
3. [Настройка переменных окружения](#3-настройка-переменных-окружения)
4. [Установка зависимостей](#4-установка-зависимостей)
5. [Структура хранения файлов](#5-структура-хранения-файлов)
6. [Использование в коде](#6-использование-в-коде)

---

## 1. Создание бакета

### Шаг 1: Вход в Yandex Cloud Console
1. Перейдите на [https://console.cloud.yandex.ru/](https://console.cloud.yandex.ru/)
2. Войдите в свой аккаунт
3. Выберите или создайте облако (cloud)

### Шаг 2: Создание бакета
1. В меню слева выберите **Object Storage**
2. Нажмите **"Создать бакет"**
3. Заполните параметры:
   - **Имя бакета**: `mariko-storage` (или другое уникальное имя)
   - **Регион**: Выберите ближайший регион (например, `ru-central1`)
   - **Тип доступа**: 
     - Для начала выберите **"Приватный"** (можно будет настроить публичный доступ позже)
   - **Класс хранилища**: `Standard` (для частого доступа)
4. Нажмите **"Создать бакет"**

### Шаг 3: Настройка публичного доступа (опционально)
Если нужно, чтобы изображения были доступны напрямую по URL:

1. Откройте созданный бакет
2. Перейдите в **"Настройки"** → **"Доступ"**
3. Включите **"Публичный доступ на чтение"**
4. Сохраните изменения

**Важно**: При включении публичного доступа все файлы в бакете будут доступны по прямому URL. Убедитесь, что это соответствует вашим требованиям безопасности.

---

## 2. Получение ключей доступа

### Шаг 1: Создание сервисного аккаунта
1. В Yandex Cloud Console перейдите в **"Сервисные аккаунты"**
2. Нажмите **"Создать сервисный аккаунт"**
3. Укажите имя: `mariko-storage-service`
4. Нажмите **"Создать"**

### Шаг 2: Назначение роли
1. Откройте созданный сервисный аккаунт
2. Перейдите на вкладку **"Роли"**
3. Нажмите **"Назначить роли"**
4. Выберите роль: **`storage.editor`** (для полного доступа к Object Storage)
5. Нажмите **"Сохранить"**

### Шаг 3: Создание статического ключа доступа
1. В сервисном аккаунте перейдите на вкладку **"Ключи"**
2. Нажмите **"Создать новый ключ"** → **"Создать статический ключ доступа"**
3. Сохраните:
   - **Access Key ID** (например: `YCAJ...`)
   - **Secret Access Key** (например: `YCM...`)

⚠️ **ВАЖНО**: Secret Access Key показывается только один раз! Сохраните его в безопасном месте.

### Альтернатива: Использование IAM-токена
Если вы предпочитаете использовать IAM-токен вместо статического ключа:
1. Создайте авторизованный ключ для сервисного аккаунта
2. Используйте его для получения IAM-токена (требует дополнительной настройки)

---

## 3. Настройка переменных окружения

Добавьте следующие переменные в файл `.env` или `.env.local`:

```bash
# Yandex Object Storage
YANDEX_STORAGE_ACCESS_KEY_ID=YCAJ...ваш_access_key_id
YANDEX_STORAGE_SECRET_ACCESS_KEY=YCM...ваш_secret_access_key
YANDEX_STORAGE_BUCKET_NAME=mariko-storage
YANDEX_STORAGE_REGION=ru-central1
YANDEX_STORAGE_ENDPOINT=https://storage.yandexcloud.net
YANDEX_STORAGE_PUBLIC_URL=https://storage.yandexcloud.net/mariko-storage
```

### Объяснение переменных:
- `YANDEX_STORAGE_ACCESS_KEY_ID` - Access Key ID из шага 2.3
- `YANDEX_STORAGE_SECRET_ACCESS_KEY` - Secret Access Key из шага 2.3
- `YANDEX_STORAGE_BUCKET_NAME` - имя вашего бакета
- `YANDEX_STORAGE_REGION` - регион бакета (например, `ru-central1`)
- `YANDEX_STORAGE_ENDPOINT` - endpoint для S3 API (обычно `https://storage.yandexcloud.net`)
- `YANDEX_STORAGE_PUBLIC_URL` - публичный URL для доступа к файлам (если включен публичный доступ)

### Обновление .env.example
Также обновите файл `.env.example` для других разработчиков:

```bash
# Yandex Object Storage
YANDEX_STORAGE_ACCESS_KEY_ID=
YANDEX_STORAGE_SECRET_ACCESS_KEY=
YANDEX_STORAGE_BUCKET_NAME=
YANDEX_STORAGE_REGION=ru-central1
YANDEX_STORAGE_ENDPOINT=https://storage.yandexcloud.net
YANDEX_STORAGE_PUBLIC_URL=
```

---

## 4. Установка зависимостей

Yandex Object Storage совместим с AWS S3 API, поэтому используем библиотеки AWS SDK:

```bash
cd backend
npm install @aws-sdk/client-s3 @aws-sdk/s3-request-presigner multer
```

Или из корня проекта:

```bash
npm install @aws-sdk/client-s3 @aws-sdk/s3-request-presigner multer --prefix backend
```

**Объяснение зависимостей:**
- `@aws-sdk/client-s3` - клиент для работы с S3-совместимым API
- `@aws-sdk/s3-request-presigner` - для генерации подписанных URL (опционально)
- `multer` - middleware для обработки загрузки файлов в Express

---

## 5. Структура хранения файлов

Рекомендуемая структура папок в бакете:

```
mariko-storage/
├── menu/
│   ├── global/              # Глобальные изображения меню (для всех ресторанов)
│   │   └── image-123.jpg
│   └── restaurant-{id}/     # Изображения для конкретного ресторана
│       └── dish-456.jpg
├── promotions/
│   ├── global/              # Глобальные баннеры акций
│   │   └── promo-789.jpg
│   └── city-{id}/           # Баннеры для конкретного города
│       └── promo-101.jpg
└── banners/                 # Другие баннеры (для будущего использования)
    └── banner-202.jpg
```

### Правила именования файлов:
- Используйте UUID или timestamp для уникальности: `{uuid}.jpg`, `{timestamp}-{original-name}.jpg`
- Сохраняйте расширение файла для правильного определения типа контента
- Избегайте пробелов и специальных символов в именах файлов

---

## 6. Использование в коде

### Пример загрузки файла:

```javascript
import { uploadFile } from './services/storageService.mjs';

const file = req.file; // из multer или другого middleware
const path = `menu/restaurant-${restaurantId}/${Date.now()}-${file.originalname}`;

const url = await uploadFile(file.buffer, path, file.mimetype);
// Вернет: https://storage.yandexcloud.net/mariko-storage/menu/restaurant-123/...
```

### Пример получения списка файлов:

```javascript
import { listFiles } from './services/storageService.mjs';

const files = await listFiles('menu/restaurant-123/');
// Вернет массив объектов с информацией о файлах
```

### Пример удаления файла:

```javascript
import { deleteFile } from './services/storageService.mjs';

await deleteFile('menu/restaurant-123/image.jpg');
```

---

## 7. Настройка CORS (если нужно)

Если вы планируете загружать файлы напрямую с фронтенда:

1. В настройках бакета перейдите в **"CORS"**
2. Добавьте правило:
   - **Разрешенные источники**: `*` или конкретные домены
   - **Разрешенные методы**: `GET`, `PUT`, `POST`, `DELETE`, `HEAD`
   - **Разрешенные заголовки**: `*`
   - **Максимальный возраст**: `3600`

---

## 8. Проверка подключения

После настройки можно проверить подключение:

1. Запустите сервер: `npm run backend:dev`
2. Попробуйте загрузить тестовое изображение через админ-панель
3. Проверьте в консоли Yandex Cloud, что файл появился в бакете

---

## 9. Безопасность

### Рекомендации:
1. ✅ **Никогда не коммитьте** `.env` файлы с реальными ключами
2. ✅ Используйте **разные ключи** для development и production
3. ✅ Регулярно **ротируйте ключи доступа**
4. ✅ Ограничьте права сервисного аккаунта только необходимыми операциями
5. ✅ Используйте **приватные бакеты** для чувствительных данных
6. ✅ Настройте **мониторинг** использования хранилища

### Ограничение доступа по IP (опционально):
В настройках бакета можно настроить политику доступа, которая ограничивает доступ по IP-адресам ваших серверов.

---

## 10. Стоимость

Yandex Object Storage тарифицируется по:
- **Хранение**: ~0.01₽ за ГБ/месяц (Standard класс)
- **Запросы**: ~0.01₽ за 1000 запросов PUT/POST
- **Исходящий трафик**: зависит от объема

Для небольших проектов обычно это очень недорого (несколько сотен рублей в месяц).

---

## 11. Дополнительные ресурсы

- [Документация Yandex Object Storage](https://cloud.yandex.ru/docs/storage/)
- [S3 API совместимость](https://cloud.yandex.ru/docs/storage/s3/)
- [AWS SDK для JavaScript](https://docs.aws.amazon.com/AWSJavaScriptSDK/v3/latest/clients/client-s3/)

---

## Готово! 🎉

После выполнения всех шагов у вас будет настроенное хранилище для загрузки изображений меню, баннеров и других файлов.
