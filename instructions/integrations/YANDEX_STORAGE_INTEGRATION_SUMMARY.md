# Сводка по интеграции Yandex Object Storage

## ✅ Что было сделано

### 1. Создана подробная инструкция
📄 **Файл:** `YANDEX_STORAGE_SETUP.md`
- Пошаговая инструкция по созданию бакета в Yandex Cloud
- Получение ключей доступа
- Настройка переменных окружения
- Структура хранения файлов
- Рекомендации по безопасности

### 2. Создан сервис для работы с хранилищем
📄 **Файл:** `backend/server/services/storageService.mjs`

**Функции:**
- `uploadFile()` - загрузка файла в хранилище
- `listFiles()` - получение списка файлов по префиксу
- `deleteFile()` - удаление файла
- `getSignedFileUrl()` - генерация подписанного URL для временного доступа
- `isStorageConfigured()` - проверка настройки хранилища

### 3. Созданы API роуты для загрузки файлов
📄 **Файл:** `backend/server/routes/storageRoutes.mjs`

**Эндпоинты:**
- `POST /api/storage/menu/:restaurantId` - загрузка изображения меню
- `GET /api/storage/menu/:restaurantId?scope=restaurant|global` - список изображений меню
- `POST /api/storage/promotions/:cityId` - загрузка изображения акции
- `GET /api/storage/promotions/:cityId?scope=city|global` - список изображений акций
- `DELETE /api/storage/:key` - удаление файла

### 4. Обновлена конфигурация
📄 **Файлы:**
- `backend/server/config.mjs` - добавлены переменные для Yandex Storage
- `backend/server/.env.example` - добавлены примеры переменных окружения
- `backend/package.json` - добавлены зависимости:
  - `@aws-sdk/client-s3`
  - `@aws-sdk/s3-request-presigner`
  - `multer`

### 5. Обновлены API на фронтенде
📄 **Файлы:**
- `frontend/src/shared/api/menuApi.ts` - реализованы функции загрузки изображений меню
- `frontend/src/shared/api/promotionsApi.ts` - реализованы функции загрузки изображений акций

### 6. Подключены роуты в сервере
📄 **Файл:** `backend/server/cart-server.mjs`
- Добавлены роуты `/api/storage` и `/api/admin/storage`

---

## 🚀 Следующие шаги

### 1. Установите зависимости
```bash
cd backend
npm install
```

### 2. Настройте переменные окружения
Следуйте инструкции в `YANDEX_STORAGE_SETUP.md`:
1. Создайте бакет в Yandex Cloud
2. Получите ключи доступа
3. Добавьте переменные в `.env` или `.env.local`:

```bash
YANDEX_STORAGE_ACCESS_KEY_ID=ваш_access_key_id
YANDEX_STORAGE_SECRET_ACCESS_KEY=ваш_secret_access_key
YANDEX_STORAGE_BUCKET_NAME=mariko-storage
YANDEX_STORAGE_REGION=ru-central1
YANDEX_STORAGE_ENDPOINT=https://storage.yandexcloud.net
YANDEX_STORAGE_PUBLIC_URL=https://storage.yandexcloud.net/mariko-storage
```

### 3. Проверьте работу
1. Запустите сервер: `npm run backend:dev`
2. Попробуйте загрузить изображение через админ-панель
3. Проверьте в консоли Yandex Cloud, что файл появился в бакете

---

## 📁 Структура хранения файлов

```
mariko-storage/
├── menu/
│   ├── global/              # Глобальные изображения меню
│   └── restaurant-{id}/     # Изображения для конкретного ресторана
├── promotions/
│   ├── global/              # Глобальные баннеры акций
│   └── city-{id}/           # Баннеры для конкретного города
└── banners/                 # Другие баннеры (для будущего использования)
```

---

## 🔒 Безопасность

- ✅ Все эндпоинты требуют авторизации администратора
- ✅ Проверка типа файла (только изображения)
- ✅ Ограничение размера файла (10MB максимум)
- ✅ Уникальные имена файлов (UUID)
- ⚠️ Не коммитьте `.env` файлы с реальными ключами

---

## 📚 Дополнительная информация

Подробная инструкция: `YANDEX_STORAGE_SETUP.md`
