# Пример файла переменных окружения для миграции БД
# Скопируйте этот файл в .env.local и заполните реальными значениями

# Подключение к исходной БД (VK Cloud PostgreSQL)
SOURCE_DATABASE_URL=postgresql://user:password@vk-cloud-host:port/database

# Подключение к целевой БД (Railway PostgreSQL)
DATABASE_URL=postgresql://user:password@railway-host:port/database

# Опционально: настройки SSL для исходной БД
# SOURCE_DATABASE_SSL=true

# Опционально: настройки SSL для целевой БД
# DATABASE_SSL=true
