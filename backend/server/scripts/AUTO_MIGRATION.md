# Автоматическая миграция базы данных

## Как это работает

При запуске приложения автоматически проверяется наличие переменной окружения `SOURCE_DATABASE_URL`.

- ✅ **Если `SOURCE_DATABASE_URL` установлена** и в целевой БД нет данных → запускается автоматическая миграция
- ❌ **Если `SOURCE_DATABASE_URL` отсутствует** → миграция не выполняется, приложение работает как обычно
- ⚠️ **Если в целевой БД уже есть данные** → миграция пропускается (защита от повторной миграции)

## Использование

### 1. Установите переменную в Railway

```bash
railway variables set SOURCE_DATABASE_URL="postgresql://user:password@vk-cloud-host:port/database" --service backend
```

### 2. Деплой

При следующем деплое приложение автоматически:
1. Обнаружит переменную `SOURCE_DATABASE_URL`
2. Проверит наличие данных в целевой БД (Railway)
3. Если данных нет - запустит миграцию
4. После успешной миграции продолжит обычный запуск

### 3. После миграции

После успешной миграции удалите переменную `SOURCE_DATABASE_URL`:

```bash
railway variables unset SOURCE_DATABASE_URL --service backend
```

Или через веб-интерфейс Railway удалите переменную вручную.

## Логирование

Процесс миграции логируется в консоль:
- `🔍 Обнаружена переменная SOURCE_DATABASE_URL` - переменная найдена
- `🚀 Запускаем автоматическую миграцию` - миграция началась
- `✅ Автоматическая миграция завершена успешно` - миграция завершена
- `ℹ️  В целевой БД уже есть данные` - миграция пропущена (данные уже есть)

## Безопасность

- Миграция запускается только если в целевой БД нет данных
- При ошибках миграции приложение продолжит работу (ошибки логируются)
- Переменная `SOURCE_DATABASE_URL` используется только для миграции и не нужна после её завершения

## Отключение автоматической миграции

Чтобы отключить автоматическую миграцию, просто удалите переменную `SOURCE_DATABASE_URL` из Railway.
