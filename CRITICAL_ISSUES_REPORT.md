# Отчет о критических проблемах приложения

Дата проверки: 2024

## 🔴 КРИТИЧЕСКИЕ ПРОБЛЕМЫ (требуют немедленного исправления)

### 1. **CORS настроен небезопасно - разрешены все origins**
**Файл:** `backend/server/cart-server.mjs:23-37`

**Проблема:**
```javascript
origin: function (origin, callback) {
  if (!origin) {
    return callback(null, true);  // ⚠️ Разрешает запросы без origin
  }
  callback(null, origin);  // ⚠️ Разрешает ЛЮБОЙ origin
}
```

**Риск:** Любой сайт может делать запросы к вашему API, что может привести к:
- Утечке данных пользователей
- CSRF атакам
- Злоупотреблению API

**Решение:** Ограничить список разрешенных origins:
```javascript
const allowedOrigins = [
  'https://ineedaglokk.ru',
  'https://your-app.vercel.app',
  // Добавить другие разрешенные домены
];

origin: function (origin, callback) {
  if (!origin || allowedOrigins.includes(origin)) {
    return callback(null, true);
  }
  callback(new Error('Not allowed by CORS'));
}
```

---

### 2. **Отсутствует валидация Telegram Init Data**
**Файл:** `backend/server/services/adminService.mjs:32-48`

**Проблема:** Telegram Init Data парсится без проверки подписи. Любой может подделать данные и получить доступ к админ-панели.

**Риск:** Полный компромисс системы безопасности админ-панели.

**Решение:** Добавить проверку подписи Telegram:
```javascript
import crypto from 'crypto';

const validateTelegramInitData = (rawData, botToken) => {
  try {
    const urlParams = new URLSearchParams(rawData);
    const hash = urlParams.get('hash');
    urlParams.delete('hash');
    
    const dataCheckString = Array.from(urlParams.entries())
      .sort(([a], [b]) => a.localeCompare(b))
      .map(([key, value]) => `${key}=${value}`)
      .join('\n');
    
    const secretKey = crypto
      .createHmac('sha256', 'WebAppData')
      .update(botToken)
      .digest();
    
    const calculatedHash = crypto
      .createHmac('sha256', secretKey)
      .update(dataCheckString)
      .digest('hex');
    
    return calculatedHash === hash;
  } catch {
    return false;
  }
};
```

---

### 3. **SQL запросы уязвимы к инъекциям (частично)**
**Файл:** `backend/server/routes/adminRoutes.mjs:202`

**Проблема:** Используется интерполяция строк в SQL запросе:
```javascript
let queryText = `SELECT * FROM ${CART_ORDERS_TABLE} WHERE 1=1`;
```

Хотя `CART_ORDERS_TABLE` берется из конфига, это плохая практика. Также есть потенциальная проблема с фильтрацией по `restaurant_id`.

**Риск:** При изменении кода может появиться SQL инъекция.

**Решение:** Использовать параметризованные запросы везде, включая имена таблиц (через whitelist).

---

### 4. **Отсутствует rate limiting для критических эндпоинтов**
**Файлы:** 
- `backend/server/routes/paymentRoutes.mjs`
- `backend/server/routes/adminRoutes.mjs`
- `backend/server/routes/cartRoutes.mjs`

**Проблема:** Нет защиты от:
- Брутфорса (попытки подбора Telegram ID)
- DDoS атак
- Злоупотребления API

**Риск:** 
- Перегрузка сервера
- Утечка данных через перебор
- Финансовые потери

**Решение:** Добавить rate limiting (например, `express-rate-limit`):
```javascript
import rateLimit from 'express-rate-limit';

const adminLimiter = rateLimit({
  windowMs: 15 * 60 * 1000, // 15 минут
  max: 100, // максимум 100 запросов
  message: 'Слишком много запросов, попробуйте позже'
});

router.use('/admin', adminLimiter);
```

---

### 5. **Webhook от YooKassa не проверяет подпись**
**Файл:** `backend/server/routes/paymentRoutes.mjs:136-190`

**Проблема:** Webhook принимает данные без проверки подписи от YooKassa.

**Риск:** Злоумышленник может подделать уведомление об оплате и получить товары бесплатно.

**Решение:** Добавить проверку подписи согласно документации YooKassa.

---

### 6. **Логи содержат чувствительные данные**
**Файл:** `backend/server/routes/adminRoutes.mjs:339`, `backend/server/cart-server.mjs:120`

**Проблема:** В логи попадают данные запросов, которые могут содержать:
- Telegram ID
- Телефоны пользователей
- Адреса доставки

**Риск:** Утечка персональных данных при доступе к логам.

**Решение:** Маскировать чувствительные данные перед логированием.

---

## 🟡 ВАЖНЫЕ ПРОБЛЕМЫ (требуют внимания)

### 7. **Отсутствует валидация входных данных на многих эндпоинтах**
**Файлы:** 
- `backend/server/routes/cartRoutes.mjs`
- `backend/server/routes/adminRoutes.mjs`

**Проблема:** Данные от клиента принимаются без валидации (например, `req.body` используется напрямую).

**Риск:** 
- Некорректные данные в БД
- Ошибки приложения
- Возможные уязвимости

**Решение:** Использовать библиотеку валидации (например, `zod` или `joi`).

---

### 8. **Нет обработки ошибок подключения к БД при старте**
**Файл:** `backend/server/cart-server.mjs:172-186`

**Проблема:** Если БД недоступна при старте, сервер все равно запускается, но многие функции не работают.

**Риск:** Непредсказуемое поведение приложения.

**Решение:** Добавить retry логику и более строгую проверку подключения.

---

### 9. **Кэш rate limiting хранится в памяти**
**Файл:** `backend/server/routes/geocodeRoutes.mjs:14`

**Проблема:** При перезапуске сервера или при горизонтальном масштабировании rate limiting не работает корректно.

**Риск:** Неэффективная защита от злоупотреблений.

**Решение:** Использовать Redis или другую распределенную систему кэширования.

---

### 10. **Отсутствует мониторинг и алертинг**
**Проблема:** Нет системы мониторинга ошибок, производительности, доступности.

**Риск:** Проблемы обнаруживаются только когда пользователи жалуются.

**Решение:** Интегрировать Sentry, DataDog или аналогичные сервисы.

---

### 11. **Нет защиты от XSS в админ-панели**
**Файл:** `frontend/src/features/admin/*`

**Проблема:** Данные от пользователей могут отображаться без санитизации.

**Риск:** XSS атаки через админ-панель.

**Решение:** Использовать библиотеки для санитизации HTML (например, `DOMPurify`).

---

### 12. **Отсутствует HTTPS enforcement**
**Проблема:** Нет проверки, что запросы приходят по HTTPS в production.

**Риск:** Перехват данных при использовании HTTP.

**Решение:** Добавить middleware для проверки HTTPS в production.

---

### 13. **Секреты могут попасть в логи**
**Файл:** `backend/server/config.mjs:168`

**Проблема:** В debug режиме логируется наличие `DATABASE_URL`, но сам URL не логируется (хорошо), однако в других местах могут логироваться секреты.

**Риск:** Утечка секретов через логи.

**Решение:** Убедиться, что нигде не логируются секреты, использовать маскирование.

---

### 14. **Нет ограничения размера тела запроса**
**Файл:** `backend/server/cart-server.mjs:40`

**Проблема:** `express.json()` используется без ограничения размера.

**Риск:** DoS атака через большие запросы.

**Решение:** Добавить `limit` опцию:
```javascript
app.use(express.json({ limit: '10mb' }));
```

---

## 🟢 РЕКОМЕНДАЦИИ ПО УЛУЧШЕНИЮ

### 15. **Добавить health checks с проверкой зависимостей**
**Файл:** `backend/server/cart-server.mjs:154-160`

**Текущее состояние:** Есть базовый health check, но он не проверяет:
- Подключение к БД
- Доступность внешних сервисов (YooKassa, iiko)
- Состояние кэша

**Рекомендация:** Расширить health check для мониторинга.

---

### 16. **Добавить структурированное логирование**
**Файл:** `backend/server/utils/logger.mjs`

**Текущее состояние:** Логирование есть, но не структурированное.

**Рекомендация:** Использовать структурированное логирование (JSON) для лучшей интеграции с системами мониторинга.

---

### 17. **Добавить тесты**
**Проблема:** Не обнаружено тестов в проекте.

**Рекомендация:** Добавить unit и integration тесты для критических компонентов.

---

### 18. **Добавить документацию API**
**Проблема:** Нет документации API (Swagger/OpenAPI).

**Рекомендация:** Добавить автоматическую генерацию документации API.

---

### 19. **Улучшить обработку ошибок**
**Проблема:** Многие места используют `console.error` вместо централизованной обработки ошибок.

**Рекомендация:** Создать централизованный error handler middleware.

---

### 20. **Добавить версионирование API**
**Проблема:** API не версионируется (нет `/api/v1/`).

**Рекомендация:** Добавить версионирование для будущих изменений без breaking changes.

---

## 📊 СТАТИСТИКА

- **Критических проблем:** 6
- **Важных проблем:** 8
- **Рекомендаций:** 6

---

## 🎯 ПРИОРИТЕТЫ ИСПРАВЛЕНИЯ

1. **Немедленно (сегодня):**
   - CORS настройка
   - Валидация Telegram Init Data
   - Проверка подписи webhook YooKassa

2. **В течение недели:**
   - Rate limiting
   - Валидация входных данных
   - Маскирование данных в логах

3. **В течение месяца:**
   - Мониторинг и алертинг
   - Тесты
   - Документация API

---

## 📝 ЗАМЕТКИ

- Проект использует современный стек технологий
- Архитектура хорошо структурирована
- Есть базовая обработка ошибок
- Требуется усиление безопасности
