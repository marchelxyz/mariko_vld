# 🚀 Инструкция по установке и настройке

## Краткий обзор вашего проекта

Отличная работа! Ваше Telegram Mini App для "Хачапури Марико" имеет превосходную архитектуру:

### ✅ Что уже готово в приложении:
- **React + TypeScript** приложение с современной архитектурой
- **Полный функционал** ресторанного бизнеса (бронирование, отзывы, профиль)
- **UI/UX библиотека** на Radix UI + TailwindCSS
- **API интеграция** готова в `src/lib/botApi.ts`
- **Система лояльности** с бонусными картами
- **Мультирестораны** - 25+ ресторанов по России
- **Responsive дизайн** для мобильных устройств

### ✅ Что я создал для интеграции:
- **Telegram Bot** на современном фреймворке Grammy
- **TypeScript конфигурация** для бота
- **Webhook и Polling** режимы работы
- **Документация** по настройке и деплою

## 📋 План установки

### Шаг 1: Установка Node.js и npm

#### Для macOS:
```bash
# Установите Homebrew (если еще не установлен)
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"

# Добавьте Homebrew в PATH (для M1 Mac)
echo 'eval "$(/opt/homebrew/bin/brew shellenv)"' >> ~/.zshrc
source ~/.zshrc

# Установите Node.js
brew install node

# Проверьте установку
node --version
npm --version
```

#### Альтернативный способ - через официальный сайт:
1. Перейдите на [nodejs.org](https://nodejs.org/)
2. Скачайте LTS версию для macOS
3. Установите через установщик

### Шаг 2: Установка зависимостей основного приложения

```bash
# В корневой директории проекта
npm install

# Запуск приложения в режиме разработки
npm run dev
```

### Шаг 3: Создание Telegram бота

1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте команду `/newbot`
3. Введите имя: `Хачапури Марико`
4. Введите username: `khachapuri_mariko_bot` (или любой свободный)
5. **Сохраните токен бота** - он понадобится

### Шаг 4: Настройка Mini App в BotFather

```
/setdescription - установите описание бота
/setabouttext - краткое описание
/setuserpic - загрузите логотип ресторана
/setmenubutton - установите кнопку меню с URL вашего приложения
```

### Шаг 5: Установка зависимостей бота

```bash
# Переходим в директорию бота
cd bot

# Устанавливаем зависимости
npm install
```

### Шаг 6: Настройка окружения бота

```bash
# Создайте файл .env в папке bot
cp env.example .env

# Отредактируйте .env файл:
nano .env
```

Содержимое `.env`:
```env
BOT_TOKEN=ваш_токен_от_botfather
WEBAPP_URL=https://ваш-домен.com
PORT=3000
NODE_ENV=development
```

### Шаг 7: Запуск системы

#### Режим разработки:
```bash
# Терминал 1: Запуск Mini App
npm run dev

# Терминал 2: Запуск бота (в папке bot)
cd bot
npm run dev
```

#### Production режим:
```bash
# Сборка приложения
npm run build

# Сборка и запуск бота
cd bot
npm run build
npm start
```

## 🌐 Деплой в production

### Вариант 1: Vercel (для фронтенда) + Heroku (для бота)

#### Фронтенд на Vercel:
```bash
# Установите Vercel CLI
npm i -g vercel

# В корневой директории
vercel

# Следуйте инструкциям
```

#### Бот на Heroku:
```bash
# Установите Heroku CLI
# Создайте приложение
heroku create khachapuri-mariko-bot

# Настройте переменные окружения
heroku config:set BOT_TOKEN=ваш_токен
heroku config:set WEBAPP_URL=https://ваше-приложение.vercel.app
heroku config:set WEBHOOK_URL=https://ваш-бот.herokuapp.com/webhook

# Деплой
git subtree push --prefix bot heroku main
```

### Вариант 2: VPS (полный контроль)

```bash
# На сервере установите Node.js, Git, PM2
sudo apt update
sudo apt install nodejs npm git
npm install -g pm2

# Клонируйте репозиторий
git clone ваш-репозиторий
cd ваш-репозиторий

# Установите зависимости
npm install
cd bot && npm install && cd ..

# Сборка
npm run build
cd bot && npm run build && cd ..

# Запуск с PM2
pm2 start bot/dist/bot.js --name khachapuri-bot
pm2 startup
pm2 save
```

## 🔧 Настройка домена и HTTPS

Для работы Telegram Mini App **обязателен HTTPS**:

1. **Купите домен** (например, на reg.ru)
2. **Настройте DNS** на ваш сервер/Vercel
3. **Установите SSL сертификат** (автоматически в Vercel/Heroku)
4. **Обновите WEBAPP_URL** в настройках бота

## 🧪 Тестирование интеграции

1. Найдите вашего бота в Telegram
2. Отправьте `/start`
3. Нажмите кнопку "🍽️ Открыть приложение"
4. Проверьте все функции приложения

## 🔗 Интеграция с CRM и внешними сервисами

Ваше приложение уже готово к интеграции с:

- **АЙКО система** - для бронирований
- **CRM системы** - для обработки отзывов
- **Платежные системы** - через Telegram Payments API
- **Аналитика** - через webhook уведомления

## 🛠 Дополнительные настройки

### Обновление HTML для Telegram WebApp:

Убедитесь, что в `index.html` есть:
```html
<script src="https://telegram.org/js/telegram-web-app.js"></script>
```

### Проверка интеграции в коде:

В `src/lib/botApi.ts` уже есть все необходимое:
- Получение данных пользователя Telegram
- Отправка уведомлений
- Работа с профилями

## 📞 Поддержка

При возникновении проблем:

1. **Проверьте логи** - `npm run dev` покажет ошибки
2. **Проверьте токен бота** - через @BotFather
3. **Убедитесь в HTTPS** - Telegram требует безопасное соединение
4. **Проверьте CORS** - если приложение на другом домене

## 🎯 Результат

После настройки у вас будет:

✅ **Полноценный Telegram Bot** с красивыми сообщениями  
✅ **Seamless интеграция** с Mini App  
✅ **Автоматическая передача** данных пользователей  
✅ **Готовность к production** деплою  
✅ **Масштабируемая архитектура** для развития  

Ваш код приложения остается **полностью нетронутым** - бот только дополняет функциональность!

---

**Создано для сети ресторанов "Хачапури Марико"** 🇬🇪 