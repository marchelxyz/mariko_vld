# Деплой из админ-панели

Теперь вы можете запускать деплой фронтенда и настройку nginx прямо из веб-интерфейса админ-панели, без необходимости вводить команды в терминале.

## Как использовать

1. **Откройте админ-панель** в веб-интерфейсе приложения
2. **Войдите как super_admin** (только супер-администраторы имеют доступ к управлению деплоем)
3. **Найдите раздел "Управление деплоем"** с иконкой ракеты 🚀
4. **Выберите нужное действие:**
   - **Деплой фронтенда** — собирает и загружает фронтенд на сервер Timeweb
   - **Настроить nginx** — настраивает конфигурацию nginx для отдачи статики
   - **Диагностика** — проверяет состояние приложения на сервере

## Что происходит при деплое

### Деплой фронтенда
- Собирает фронтенд локально (`npm run frontend:build`)
- Загружает файлы в `/var/www/html` на сервере Timeweb
- Устанавливает зависимости backend и перезапускает pm2 процессы
- Устанавливает правильные права доступа на файлы

### Настройка nginx
- Копирует конфигурацию nginx на сервер
- Включает конфигурацию через symlink
- Проверяет конфигурацию и перезагружает nginx
- Проверяет доступность фронтенда

### Диагностика
- Проверяет статус nginx
- Проверяет наличие файлов фронтенда
- Проверяет права доступа
- Проверяет статус backend процессов
- Проверяет доступность фронтенда и API

## Требования

1. **Файл `.env.deploy`** должен существовать в корне проекта
2. **SSH доступ** к серверу Timeweb должен быть настроен (по ключу или паролю)
3. **Права super_admin** в админ-панели

## Настройка .env.deploy

Убедитесь, что файл `.env.deploy` содержит правильные настройки:

```bash
SERVER_HOST="root@85.198.83.72"
WEB_ROOT="/var/www/html"
REMOTE_PROJECT_ROOT="/root/HM-projecttt"
# ... другие настройки
```

## Безопасность

- Доступ к управлению деплоем имеют только **super_admin**
- Все операции логируются на сервере
- Вывод операций отображается в интерфейсе для контроля

## Устранение проблем

Если деплой не работает:

1. **Проверьте файл `.env.deploy`** — он должен существовать и содержать правильные настройки
2. **Проверьте SSH доступ** — убедитесь, что сервер доступен по SSH
3. **Проверьте логи** — вывод операции отображается в интерфейсе
4. **Запустите диагностику** — она покажет текущее состояние сервера

## Альтернативный способ

Если веб-интерфейс недоступен, вы можете использовать скрипты напрямую:

```bash
# Деплой фронтенда
DEPLOY_ENV_FILE=.env.deploy bash scripts/deploy-local.sh

# Настройка nginx
DEPLOY_ENV_FILE=.env.deploy bash scripts/setup-timeweb-nginx.sh

# Диагностика
DEPLOY_ENV_FILE=.env.deploy bash scripts/diagnose-timeweb.sh
```
